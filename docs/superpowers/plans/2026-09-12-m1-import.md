# M1 轨迹导入 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让轨迹数据进得来——网页上传单个 GPX + 批量导入 GeoLife `.plt` 目录，解析、清洗、标记异常点后入库。

**Architecture:** 统一的 `Importer` 接口（三种格式 = 三个实现）输出同一种中间结构 `RawPoint`，清洗规则集中在 `TrackCleaner` 一处，`ImportService` 负责编排「识别格式 → 解析 → 清洗 → 分批入库」。两个入口（网页上传、目录批量）共用同一套管道。

**Tech Stack:** Spring Boot 3.5.16 / Java 25 / JUnit 5（本地 `.m2` 已有）/ JDK 自带 DOM 解析 XML（零新增依赖）/ PostGIS

**上游设计文档：** `docs/superpowers/specs/2026-09-12-m1-import-design.md`

---

## 文件结构

| 文件 | 职责 |
| --- | --- |
| `scripts/db/01-schema.sql` | **改**：`track_point` 加 `is_outlier` 列（两处写法） |
| `backend/src/main/java/com/calcite/domain/TrackPoint.java` | **改**：加 `isOutlier` 字段 + 公开构造器 |
| `backend/src/main/java/com/calcite/domain/Track.java` | **改**：加公开构造器（原来只有 protected） |
| `backend/src/main/java/com/calcite/service/GeoUtils.java` | **新建**：球面距离（Haversine） |
| `backend/src/main/java/com/calcite/service/TrackCleaner.java` | **新建**：清洗 + 标记（规则只有这一份） |
| `backend/src/main/java/com/calcite/service/ImportService.java` | **新建**：编排全流程 |
| `backend/src/main/java/com/calcite/service/importer/RawPoint.java` | **新建**：统一中间结构 |
| `backend/src/main/java/com/calcite/service/importer/ParsedTrack.java` | **新建**：解析结果 |
| `backend/src/main/java/com/calcite/service/importer/Importer.java` | **新建**：解析器接口 |
| `backend/src/main/java/com/calcite/service/importer/FormatDetector.java` | **新建**：按内容识别格式 |
| `backend/src/main/java/com/calcite/service/importer/GpxImporter.java` | **新建**：GPX 解析（JDK DOM） |
| `backend/src/main/java/com/calcite/service/importer/GeoLifeImporter.java` | **新建**：`.plt` 解析 |
| `backend/src/main/java/com/calcite/web/dto/ImportResult.java` | **新建**：导入结果 DTO |
| `backend/src/main/java/com/calcite/config/ImportProperties.java` | **新建**：绑定 `calcite.import.*` 配置（YAML 列表必须用 `@ConfigurationProperties`，`@Value` 绑不了） |
| `backend/src/main/java/com/calcite/web/ImportController.java` | **新建**：两个导入接口 |
| `backend/src/main/java/com/calcite/web/dto/TrackPointDto.java` | **改**：暴露 `isOutlier` |
| `backend/src/main/resources/application.yml` | **改**：导入配置 + 批量插入参数 |
| `backend/src/test/resources/sample.gpx` | **新建**：真实 GPX 夹具（复制自用户数据） |
| `backend/src/test/resources/sample.plt` | **新建**：自造 `.plt` 夹具 |
| `backend/src/test/java/com/calcite/service/GeoUtilsTest.java` | **新建** |
| `backend/src/test/java/com/calcite/service/TrackCleanerTest.java` | **新建** |
| `backend/src/test/java/com/calcite/service/importer/FormatDetectorTest.java` | **新建** |
| `backend/src/test/java/com/calcite/service/importer/GpxImporterTest.java` | **新建** |
| `backend/src/test/java/com/calcite/service/importer/GeoLifeImporterTest.java` | **新建** |
| `frontend/src/components/TrackList.vue` | **改**：导入按钮 + `emit('import', file)` |
| `frontend/src/App.vue` | **改**：上传逻辑 + 摘要显示 |
| `docs/superpowers/specs/2026-09-08-calcite-trajectory-analysis-design.md` | **改**：同步 4 处差异（Task 10） |
| `_session_context.md` | **改**：记录 M1 完成（Task 10） |

### 命名约定（后面所有任务统一使用，不要改名）

```java
record RawPoint(double lat, double lon, Double elevationM, OffsetDateTime recordedAt)
record ParsedTrack(String name, List<RawPoint> points)
interface Importer { String format(); ParsedTrack parse(InputStream in) throws Exception; }
final class FormatDetector { static String detect(byte[] head) }        // 返回 "gpx" / "geolife" / "unknown"
final class GeoUtils { static double haversineMeters(double lat1, double lon1, double lat2, double lon2) }
class TrackCleaner { CleanedTrack clean(List<RawPoint> raw) }
record CleanedTrack(List<RawPoint> points, List<Double> speeds, List<Boolean> outliers, double distanceM, int durationS)
class ImportService { ImportResult importBytes(byte[] content, String fallbackName); GeoLifeImportResult importGeoLifeDirectory(Path root, int maxTracks) }
```

### 常用命令（每个任务都会用到，先记在这里）

```powershell
# 跑后端测试（必须在仓库根目录）
& "E:\JAVA_IDEA_package\JAVA_IDEA_app\IntelliJ IDEA 2026.1\plugins\maven\lib\maven3\bin\mvn.cmd" -B "-Dmaven.repo.local=E:\JAVA_IDEA_package\JAVA_Project\Calcite\.m2\repository" -f "E:\JAVA_IDEA_package\JAVA_Project\Calcite\backend\pom.xml" test

# 只跑某一个测试类
& "...\mvn.cmd" -B "-Dmaven.repo.local=..." -f "...\backend\pom.xml" test "-Dtest=GpxImporterTest"

# 跑 SQL 脚本
$env:PGPASSWORD='见 application-local.yml'; $env:LC_MESSAGES='C'
& "E:\PostgreSQL\bin\psql.exe" -U postgres -d calcite -P pager=off -f scripts\db\01-schema.sql
```

---

## Task 1: 数据表加列 + 实体补齐构造器

**Files:**
- Modify: `scripts/db/01-schema.sql`
- Modify: `backend/src/main/java/com/calcite/domain/TrackPoint.java`
- Modify: `backend/src/main/java/com/calcite/domain/Track.java`

- [ ] **Step 1: 给 `track_point` 加 `is_outlier` 列（两处）**

打开 `scripts/db/01-schema.sql`。找到 `CREATE TABLE IF NOT EXISTS track_point`，在 `geom` 那一行**之前**插入一列：

```sql
  speed_mps   DOUBLE PRECISION,
  is_outlier  BOOLEAN     NOT NULL DEFAULT false,   -- 疑似 GPS 漂移点（导入时标记）
  geom        GEOMETRY(Point, 4326) NOT NULL
```

然后在**文件末尾**（最后一个 `\echo ''` 之后）追加：

```sql
-- 老库补列：CREATE TABLE IF NOT EXISTS 不会给已存在的表加列，所以必须单独写一句。
-- 加上它就保持了「整个脚本重跑一遍就对齐」的特性。
ALTER TABLE track_point ADD COLUMN IF NOT EXISTS is_outlier BOOLEAN NOT NULL DEFAULT false;
```

- [ ] **Step 2: 跑脚本并验证列已存在**

```powershell
$env:PGPASSWORD='见 application-local.yml'; $env:LC_MESSAGES='C'
& "E:\PostgreSQL\bin\psql.exe" -U postgres -d calcite -P pager=off -c "SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name='track_point' ORDER BY ordinal_position;"
```

Expected：输出里有 `is_outlier | boolean | NO` 一行。

- [ ] **Step 3: `TrackPoint` 加字段**

在 `TrackPoint.java` 的 `speedMps` 字段之后插入：

```java
    /** 疑似 GPS 漂移点，导入时由 TrackCleaner 标记 */
    @Column(name = "is_outlier", nullable = false)
    private boolean outlier = false;
```

在 `getSpeedMps()` 之后插入 getter/setter（**注意字段名 `outlier`，属性名 `isOutlier`**）：

```java
    public boolean isOutlier() {
        return outlier;
    }

    public void setOutlier(boolean outlier) {
        this.outlier = outlier;
    }
```

- [ ] **Step 4: 给 `TrackPoint` 加公开构造器**

把原来的 `protected TrackPoint() { }` **替换**为：

```java
    /** JPA 用 */
    protected TrackPoint() {
    }

    /**
     * 导入时用。
     *
     * <p>原来只有 protected 构造器（因为数据一直是 SQL 插的，Java 从没创建过 TrackPoint），
     * 导入功能是第一个需要在 Java 里 new 出轨迹点的地方，所以补一个 public 的。
     */
    public TrackPoint(Long trackId, Integer seq, OffsetDateTime recordedAt,
                      Double elevationM, Double speedMps, Point geom, boolean outlier) {
        this.trackId = trackId;
        this.seq = seq;
        this.recordedAt = recordedAt;
        this.elevationM = elevationM;
        this.speedMps = speedMps;
        this.geom = geom;
        this.outlier = outlier;
    }
```

- [ ] **Step 5: 给 `Track` 加公开构造器**

在 `Track.java` 里，把原来的：

```java
    protected Track() {
        // JPA 需要一个无参构造器（可以是 protected，不需要给业务代码用）
    }
```

**替换**为：

```java
    protected Track() {
        // JPA 需要一个无参构造器（可以是 protected，不需要给业务代码用）
    }

    /**
     * 导入时用。派生字段（距离/时长/点数/线）由 ImportService 算好后 set 进来。
     */
    public Track(String name, String source, String externalId,
                 OffsetDateTime startTime, OffsetDateTime endTime) {
        this.name = name;
        this.source = source;
        this.externalId = externalId;
        this.startTime = startTime;
        this.endTime = endTime;
    }
```

- [ ] **Step 6: 编译验证**

```powershell
& "...\mvn.cmd" -B "-Dmaven.repo.local=..." -f "...\backend\pom.xml" -q compile
```

Expected：无输出（成功）。若有 `cannot find symbol` 说明字段名写错。

- [ ] **Step 7: 提交**

```bash
git add scripts/db/01-schema.sql backend/src/main/java/com/calcite/domain/Track.java backend/src/main/java/com/calcite/domain/TrackPoint.java
git commit -m "feat(domain): track_point 加 is_outlier 列，实体补公开构造器

导入功能是第一个需要在 Java 里创建 Track/TrackPoint 的地方，
原来只有 protected 构造器（数据一直是 SQL 插的）。"
```

---

## Task 2: 格式识别（纯函数，TDD）

**Files:**
- Create: `backend/src/main/java/com/calcite/service/importer/FormatDetector.java`
- Test: `backend/src/test/java/com/calcite/service/importer/FormatDetectorTest.java`

- [ ] **Step 1: 写失败的测试**

创建 `backend/src/test/java/com/calcite/service/importer/FormatDetectorTest.java`：

```java
package com.calcite.service.importer;

import org.junit.jupiter.api.Test;

import java.nio.charset.StandardCharsets;

import static org.junit.jupiter.api.Assertions.assertEquals;

class FormatDetectorTest {

    private static byte[] bytes(String s) {
        return s.getBytes(StandardCharsets.UTF_8);
    }

    @Test
    void 识别_gpx_即使带_bom_和单引号声明() {
        String gpx = "<?xml version='1.0' encoding='UTF-8' standalone='yes' ?>\n"
                + "<gpx creator=\"vivo运动健康\"><trk><trkseg>"
                + "<trkpt lat=\"25.03\" lon=\"117.02\"/></trkseg></trk></gpx>";
        assertEquals("gpx", FormatDetector.detect(bytes(gpx)));
    }

    @Test
    void 识别_gpx_无_xml_声明() {
        assertEquals("gpx", FormatDetector.detect(bytes("<gpx version=\"1.0\"></gpx>")));
    }

    @Test
    void 识别_geolife_plt() {
        String plt = "39.984702,116.318417,0,492,39744.1201851852,2008-10-23,02:53:04\n"
                + "39.984683,116.318450,0,492,39744.1202546296,2008-10-23,02:53:05";
        assertEquals("geolife", FormatDetector.detect(bytes(plt)));
    }

    @Test
    void 无法识别时返回_unknown() {
        assertEquals("unknown", FormatDetector.detect(bytes("这不是轨迹数据")));
        assertEquals("unknown", FormatDetector.detect(bytes("name,age\n张三,20")));
        assertEquals("unknown", FormatDetector.detect(new byte[0]));
    }

    @Test
    void 按内容识别而不是扩展名_真实样本是_bin_tmp() {
        // 用户那份文件的扩展名是 .gpx.bin_tmp，检测只看内容，所以照样识别成 gpx
        String gpx = "<?xml version='1.0' encoding='UTF-8' standalone='yes' ?><gpx></gpx>";
        assertEquals("gpx", FormatDetector.detect(bytes(gpx)));
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

```powershell
& "...\mvn.cmd" -B "-Dmaven.repo.local=..." -f "...\backend\pom.xml" test "-Dtest=FormatDetectorTest"
```

Expected: **编译失败**，报 `cannot find symbol: class FormatDetector`。

- [ ] **Step 3: 实现 `FormatDetector`**

创建 `backend/src/main/java/com/calcite/service/importer/FormatDetector.java`：

```java
package com.calcite.service.importer;

import java.nio.charset.StandardCharsets;

/**
 * 按【文件内容】识别格式 —— 不看扩展名。
 *
 * <p>为什么强调这一点：实测样本的文件名是 {@code 2026年6月22日户外跑步.gpx.bin_tmp}，
 * 扩展名根本不是 .gpx。按扩展名判断会把它直接拒掉。
 */
public final class FormatDetector {

    private FormatDetector() {
    }

    /**
     * @param head 文件开头的一段字节（几百字节就够）
     * @return {@code "gpx"} / {@code "geolife"} / {@code "unknown"}
     */
    public static String detect(byte[] head) {
        if (head == null || head.length == 0) {
            return "unknown";
        }
        String text = new String(head, StandardCharsets.UTF_8).trim();
        if (text.isEmpty()) {
            return "unknown";
        }

        // GPX：XML 文档，可能带 <?xml 声明，也可能直接以 <gpx 开头
        if (text.startsWith("<?xml") || text.startsWith("<gpx") || text.contains("<gpx")) {
            return "gpx";
        }

        // GeoLife .plt：每行 7 个逗号分隔字段，前两个是纬度和经度
        for (String line : text.split("\\R")) {
            String s = line.trim();
            if (s.isEmpty()) {
                continue;
            }
            String[] f = s.split(",");
            if (f.length == 7 && isNumber(f[0]) && isNumber(f[1])) {
                return "geolife";
            }
            break; // 只看第一行有效内容
        }

        return "unknown";
    }

    private static boolean isNumber(String s) {
        try {
            Double.parseDouble(s.trim());
            return true;
        } catch (NumberFormatException e) {
            return false;
        }
    }
}
```

- [ ] **Step 4: 跑测试确认通过**

```powershell
& "...\mvn.cmd" -B "-Dmaven.repo.local=..." -f "...\backend\pom.xml" test "-Dtest=FormatDetectorTest"
```

Expected: `Tests run: 5, Failures: 0, Errors: 0, Skipped: 0` + `BUILD SUCCESS`。

- [ ] **Step 5: 提交**

```bash
git add backend/src/main/java/com/calcite/service/importer/FormatDetector.java backend/src/test/java/com/calcite/service/importer/FormatDetectorTest.java
git commit -m "feat(import): 按文件内容识别格式（不看扩展名）+ 5 项测试"
```

---

## Task 3: 中间结构与解析器接口

**Files:**
- Create: `backend/src/main/java/com/calcite/service/importer/RawPoint.java`
- Create: `backend/src/main/java/com/calcite/service/importer/ParsedTrack.java`
- Create: `backend/src/main/java/com/calcite/service/importer/Importer.java`

这三个是**纯数据定义 + 接口**，没有逻辑，所以不需要测试（测试它们的 getter 是浪费）。

- [ ] **Step 1: 创建 `RawPoint`**

```java
package com.calcite.service.importer;

import java.time.OffsetDateTime;

/**
 * 解析器统一输出的中间结构：一个原始点。
 *
 * <p>存在的意义：GPX 和 .plt 的格式差异在各自的解析器里消化掉，
 * 吐出来全是这一种东西 —— 于是清洗规则（TrackCleaner）只需要写一份。
 *
 * @param elevationM 海拔（米）；数据源没有就是 null
 */
public record RawPoint(double lat, double lon, Double elevationM, OffsetDateTime recordedAt) {
}
```

- [ ] **Step 2: 创建 `ParsedTrack`**

```java
package com.calcite.service.importer;

import java.util.List;

/**
 * 一个解析器的产出。
 *
 * @param name  轨迹名，可能为 null（GPX 没有 &lt;name&gt;、.plt 根本没有名字字段），
 *              为 null 时由 ImportService 用文件名兜底
 * @param points 原始点，**未排序、未清洗**
 */
public record ParsedTrack(String name, List<RawPoint> points) {
}
```

- [ ] **Step 3: 创建 `Importer`**

```java
package com.calcite.service.importer;

import java.io.InputStream;

/**
 * 一种轨迹文件格式的解析器。
 *
 * <p>实现类必须无状态、可复用（不要往实现类里存字段）。
 */
public interface Importer {

    /** 格式标识，与 {@link FormatDetector#detect} 的返回值一致：{@code "gpx"} / {@code "geolife"} */
    String format();

    /**
     * 从字节流解析。
     *
     * <p>实现应该容忍个别坏点（跳过并继续），但整体格式不对时直接抛异常。
     */
    ParsedTrack parse(InputStream in) throws Exception;
}
```

- [ ] **Step 4: 编译验证**

```powershell
& "...\mvn.cmd" -B "-Dmaven.repo.local=..." -f "...\backend\pom.xml" -q compile
```

Expected：无输出。

- [ ] **Step 5: 提交**

```bash
git add backend/src/main/java/com/calcite/service/importer/RawPoint.java backend/src/main/java/com/calcite/service/importer/ParsedTrack.java backend/src/main/java/com/calcite/service/importer/Importer.java
git commit -m "feat(import): 解析器接口 + RawPoint 统一中间结构"
```

---

## Task 4: GPX 解析器（TDD，用真实样本）

**Files:**
- Create: `backend/src/test/resources/sample.gpx`
- Create: `backend/src/main/java/com/calcite/service/importer/GpxImporter.java`
- Test: `backend/src/test/java/com/calcite/service/importer/GpxImporterTest.java`

- [ ] **Step 1: 把用户的真实 GPX 复制成测试夹具**

```powershell
Copy-Item "D:\Calcite-note\GPX-Data\2026年6月22日户外跑步.gpx.bin_tmp" "E:\JAVA_IDEA_package\JAVA_Project\Calcite\backend\src\test\resources\sample.gpx" -Force
```

（**故意保留原始文件名信息不上传**：夹具就叫 `sample.gpx`，因为内容识别不依赖文件名。）

- [ ] **Step 2: 写失败的测试**

创建 `backend/src/test/java/com/calcite/service/importer/GpxImporterTest.java`：

```java
package com.calcite.service.importer;

import org.junit.jupiter.api.Test;

import java.io.InputStream;
import java.time.OffsetDateTime;
import java.util.List;

import static org.junit.jupiter.api.Assertions.*;

class GpxImporterTest {

    private ParsedTrack parseSample() throws Exception {
        try (InputStream in = getClass().getResourceAsStream("/sample.gpx")) {
            assertNotNull(in, "找不到 /sample.gpx 夹具");
            return new GpxImporter().parse(in);
        }
    }

    @Test
    void 解析真实样本_点数与首末时间正确() throws Exception {
        ParsedTrack t = parseSample();
        List<RawPoint> pts = t.points();

        assertEquals(2342, pts.size(), "真实样本应有 2342 个点");
        assertEquals(OffsetDateTime.parse("2026-06-22T11:00:08Z"), pts.get(0).recordedAt());
        assertEquals(OffsetDateTime.parse("2026-06-22T11:39:16Z"), pts.get(pts.size() - 1).recordedAt());
    }

    @Test
    void 真实样本的坐标与海拔() throws Exception {
        List<RawPoint> pts = parseSample().points();

        assertEquals(25.0342562, pts.get(0).lat(), 1e-6);
        assertEquals(117.0204852, pts.get(0).lon(), 1e-6);
        // vivo 这份数据海拔全是 0.0（不是没解析出来）
        assertNotNull(pts.get(0).elevationM());
        assertEquals(0.0, pts.get(0).elevationM(), 1e-9);
    }

    @Test
    void 样本没有_name_所以名字为_null_由服务层兜底() throws Exception {
        assertNull(parseSample().name());
    }

    @Test
    void 能解析带_name_的_gpx() throws Exception {
        String gpx = "<?xml version='1.0' encoding='UTF-8'?>"
                + "<gpx xmlns=\"http://www.topografix.com/GPX/1/0\">"
                + "<trk><name>晨跑</name><trkseg>"
                + "<trkpt lat=\"39.9\" lon=\"116.3\"><ele>50.5</ele><time>2008-10-23T02:53:04Z</time></trkpt>"
                + "<trkpt lat=\"39.8\" lon=\"116.4\"><ele>52.0</ele><time>2008-10-23T02:53:05Z</time></trkpt>"
                + "</trkseg></trk></gpx>";
        ParsedTrack t = new GpxImporter().parse(new java.io.ByteArrayInputStream(gpx.getBytes(java.nio.charset.StandardCharsets.UTF_8)));

        assertEquals("晨跑", t.name());
        assertEquals(2, t.points().size());
        assertEquals(50.5, t.points().get(0).elevationM(), 1e-9);
        assertEquals(39.9, t.points().get(0).lat(), 1e-9);
    }

    @Test
    void 单个坏点被跳过_不影响整体() throws Exception {
        String gpx = "<?xml version='1.0' encoding='UTF-8'?>"
                + "<gpx><trk><trkseg>"
                + "<trkpt lat=\"39.9\" lon=\"116.3\"><time>2008-10-23T02:53:04Z</time></trkpt>"
                + "<trkpt lat=\"BAD\" lon=\"116.4\"><time>2008-10-23T02:53:05Z</time></trkpt>"
                + "<trkpt lat=\"39.7\" lon=\"116.5\"><time>2008-10-23T02:53:06Z</time></trkpt>"
                + "</trkseg></trk></gpx>";
        ParsedTrack t = new GpxImporter().parse(new java.io.ByteArrayInputStream(gpx.getBytes(java.nio.charset.StandardCharsets.UTF_8)));

        assertEquals(2, t.points().size());
    }

    @Test
    void 不是_gpx_时抛异常() {
        String bad = "{\"not\":\"gpx\"}";
        assertThrows(Exception.class, () -> new GpxImporter().parse(
                new java.io.ByteArrayInputStream(bad.getBytes(java.nio.charset.StandardCharsets.UTF_8))));
    }
}
```

- [ ] **Step 3: 跑测试确认失败**

```powershell
& "...\mvn.cmd" -B "-Dmaven.repo.local=..." -f "...\backend\pom.xml" test "-Dtest=GpxImporterTest"
```

Expected: 编译失败，`cannot find symbol: class GpxImporter`。

- [ ] **Step 4: 实现 `GpxImporter`**

创建 `backend/src/main/java/com/calcite/service/importer/GpxImporter.java`：

```java
package com.calcite.service.importer;

import org.w3c.dom.Document;
import org.w3c.dom.Element;
import org.w3c.dom.Node;
import org.w3c.dom.NodeList;

import javax.xml.parsers.DocumentBuilderFactory;
import java.io.InputStream;
import java.time.OffsetDateTime;
import java.util.ArrayList;
import java.util.List;

/**
 * GPX 解析器。
 *
 * <p>用 JDK 自带的 DOM，不引入任何新依赖 —— 和项目里"手写 SVG 不装图表库"是同一个取舍。
 * GPX 文件很小（实测 300 KB），一次性读进内存没问题。
 */
public class GpxImporter implements Importer {

    private static final String GPX_NS = "http://www.topografix.com/GPX/1/0";

    @Override
    public String format() {
        return "gpx";
    }

    @Override
    public ParsedTrack parse(InputStream in) throws Exception {
        DocumentBuilderFactory factory = DocumentBuilderFactory.newInstance();
        factory.setNamespaceAware(true);
        // GPX 来自外部文件，必须关掉外部实体解析，否则会有 XXE 漏洞
        factory.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true);
        factory.setFeature("http://xml.org/sax/features/external-general-entities", false);
        factory.setFeature("http://xml.org/sax/features/external-parameter-entities", false);

        Document doc = factory.newDocumentBuilder().parse(in);

        String name = firstText(doc, "trk", "name");

        List<RawPoint> points = new ArrayList<>();
        NodeList nodes = doc.getElementsByTagNameNS("*", "trkpt");
        for (int i = 0; i < nodes.getLength(); i++) {
            Element el = (Element) nodes.item(i);
            try {
                double lat = Double.parseDouble(el.getAttribute("lat"));
                double lon = Double.parseDouble(el.getAttribute("lon"));
                if (lat < -90 || lat > 90 || lon < -180 || lon > 180) {
                    continue; // 坐标越界的点直接跳过
                }
                Double ele = parseDoubleOrNull(firstChildText(el, "ele"));
                OffsetDateTime t = parseTime(firstChildText(el, "time"));
                if (t == null) {
                    continue; // 没有时间的点无法参与速度计算，跳过
                }
                points.add(new RawPoint(lat, lon, ele, t));
            } catch (RuntimeException ignored) {
                // 单个坏点跳过，不影响整条轨迹
            }
        }

        if (points.isEmpty()) {
            throw new IllegalArgumentException("文件里没有有效的轨迹点");
        }
        return new ParsedTrack(name, points);
    }

    /** 取 {@code <parent><child>} 的文本；兼容带命名空间和不带命名空间两种写法 */
    private static String firstText(Document doc, String parent, String child) {
        NodeList parents = doc.getElementsByTagNameNS("*", parent);
        for (int i = 0; i < parents.getLength(); i++) {
            String v = firstChildText((Element) parents.item(i), child);
            if (v != null && !v.isBlank()) {
                return v.trim();
            }
        }
        return null;
    }

    private static String firstChildText(Element parent, String tag) {
        NodeList kids = parent.getChildNodes();
        for (int i = 0; i < kids.getLength(); i++) {
            Node n = kids.item(i);
            if (n.getNodeType() == Node.ELEMENT_NODE && tag.equals(n.getLocalName())) {
                String v = n.getTextContent();
                if (v != null && !v.isBlank()) {
                    return v.trim();
                }
            }
        }
        return null;
    }

    private static Double parseDoubleOrNull(String s) {
        if (s == null) {
            return null;
        }
        try {
            return Double.parseDouble(s);
        } catch (NumberFormatException e) {
            return null;
        }
    }

    private static OffsetDateTime parseTime(String s) {
        if (s == null) {
            return null;
        }
        try {
            return OffsetDateTime.parse(s);
        } catch (RuntimeException e) {
            return null;
        }
    }
}
```

- [ ] **Step 5: 跑测试确认通过**

```powershell
& "...\mvn.cmd" -B "-Dmaven.repo.local=..." -f "...\backend\pom.xml" test "-Dtest=GpxImporterTest"
```

Expected: `Tests run: 6, Failures: 0, Errors: 0`。

若报 `找不到 /sample.gpx 夹具`，检查 Step 1 的复制是否成功（`backend/src/test/resources/sample.gpx`）。

- [ ] **Step 6: 提交**

```bash
git add backend/src/test/resources/sample.gpx backend/src/main/java/com/calcite/service/importer/GpxImporter.java backend/src/test/java/com/calcite/service/importer/GpxImporterTest.java
git commit -m "feat(import): GPX 解析器（JDK DOM 零依赖）+ 用真实样本做夹具"
```

---

## Task 5: GeoLife `.plt` 解析器（TDD）

**Files:**
- Create: `backend/src/test/resources/sample.plt`
- Create: `backend/src/main/java/com/calcite/service/importer/GeoLifeImporter.java`
- Test: `backend/src/test/java/com/calcite/service/importer/GeoLifeImporterTest.java`

### `.plt` 格式说明（写代码前必须理解）

每行 7 个逗号分隔字段：

```
39.984702,116.318417,0,492,39744.1201851852,2008-10-23,02:53:04
  纬度      经度    保留  海拔(英尺) 1899年以来的天数  日期      时间(UTC)
```

- **海拔单位是英尺**，必须 × 0.3048 转成米
- 第 5 个字段 `39744.1201851852` 是「1899-12-30 00:00:00 UTC 起的天数（含小数）」，它和第 6/7 个字段是**一致的**：`0.1201851852 天 × 86400 = 10384 秒 = 02:53:04`。
  所以我们**用第 6/7 个字段**（可读、无浮点误差），把第 5 个字段只当作交叉校验。
- 有些版本第 3 个字段是 `0`（无意义的占位）

- [ ] **Step 1: 创建自造夹具**

创建 `backend/src/test/resources/sample.plt`（**内容必须精确，测试会断言具体数值**）：

```
39.984702,116.318417,0,492,39744.1201851852,2008-10-23,02:53:04
39.984683,116.318450,0,492,39744.1202546296,2008-10-23,02:53:05
39.984686,116.318417,0,492,39744.1203240741,2008-10-23,02:53:06
39.984688,116.318385,0,520,39744.1203935185,2008-10-23,02:53:07
```

- [ ] **Step 2: 写失败的测试**

创建 `backend/src/test/java/com/calcite/service/importer/GeoLifeImporterTest.java`：

```java
package com.calcite.service.importer;

import org.junit.jupiter.api.Test;

import java.io.ByteArrayInputStream;
import java.io.InputStream;
import java.nio.charset.StandardCharsets;
import java.time.OffsetDateTime;
import java.util.List;

import static org.junit.jupiter.api.Assertions.*;

class GeoLifeImporterTest {

    private ParsedTrack parseFixture() throws Exception {
        try (InputStream in = getClass().getResourceAsStream("/sample.plt")) {
            assertNotNull(in, "找不到 /sample.plt 夹具");
            return new GeoLifeImporter().parse(in);
        }
    }

    @Test
    void 解析样本_点数与首点内容正确() throws Exception {
        List<RawPoint> pts = parseFixture().points();

        assertEquals(4, pts.size());
        RawPoint p0 = pts.get(0);
        assertEquals(39.984702, p0.lat(), 1e-9);
        assertEquals(116.318417, p0.lon(), 1e-9);
        assertEquals(OffsetDateTime.parse("2008-10-23T02:53:04Z"), p0.recordedAt());
    }

    @Test
    void 海拔从英尺换算成米() throws Exception {
        List<RawPoint> pts = parseFixture().points();

        // 492 英尺 × 0.3048 = 149.9616 米
        assertEquals(149.9616, pts.get(0).elevationM(), 1e-4);
        // 第四行是 520 英尺
        assertEquals(158.496, pts.get(3).elevationM(), 1e-4);
    }

    @Test
    void 时间字段与天数字段一致() throws Exception {
        // 39744.1201851852 天（自 1899-12-30 UTC）应等于 2008-10-23T02:53:04Z
        OffsetDateTime fromDays = OffsetDateTime.parse("1899-12-30T00:00:00Z")
                .plusSeconds(Math.round(39744.1201851852 * 86400));
        assertEquals(fromDays, parseFixture().points().get(0).recordedAt());
    }

    @Test
    void plt_没有轨迹名_名字为_null() throws Exception {
        assertNull(parseFixture().name());
    }

    @Test
    void 空行和字段不足的行被跳过() throws Exception {
        String plt = "\n"
                + "39.984702,116.318417,0,492,39744.1201851852,2008-10-23,02:53:04\n"
                + "坏行\n"
                + "39.984683,116.318450,0,492,39744.1202546296,2008-10-23,02:53:05\n"
                + "\n";
        ParsedTrack t = new GeoLifeImporter().parse(
                new ByteArrayInputStream(plt.getBytes(StandardCharsets.UTF_8)));

        assertEquals(2, t.points().size());
    }

    @Test
    void 全空文件抛异常() {
        assertThrows(Exception.class, () -> new GeoLifeImporter().parse(
                new ByteArrayInputStream("\n\n".getBytes(StandardCharsets.UTF_8))));
    }
}
```

- [ ] **Step 3: 跑测试确认失败**

```powershell
& "...\mvn.cmd" -B "-Dmaven.repo.local=..." -f "...\backend\pom.xml" test "-Dtest=GeoLifeImporterTest"
```

Expected: 编译失败，`cannot find symbol: class GeoLifeImporter`。

- [ ] **Step 4: 实现 `GeoLifeImporter`**

创建 `backend/src/main/java/com/calcite/service/importer/GeoLifeImporter.java`：

```java
package com.calcite.service.importer;

import java.io.BufferedReader;
import java.io.InputStream;
import java.io.InputStreamReader;
import java.nio.charset.StandardCharsets;
import java.time.OffsetDateTime;
import java.util.ArrayList;
import java.util.List;

/**
 * GeoLife 数据集的 {@code .plt} 解析器。
 *
 * <p>格式：{@code 纬度,经度,保留,海拔(英尺),1899年以来的天数,日期,时间}
 *
 * @see <a href="https://www.geos.ed.ac.uk/~yliu23/geolife/">GeoLife 数据说明</a>
 */
public class GeoLifeImporter implements Importer {

    /** 英尺 → 米 */
    private static final double FEET_TO_METER = 0.3048;

    @Override
    public String format() {
        return "geolife";
    }

    @Override
    public ParsedTrack parse(InputStream in) throws Exception {
        List<RawPoint> points = new ArrayList<>();

        try (BufferedReader reader = new BufferedReader(new InputStreamReader(in, StandardCharsets.UTF_8))) {
            String line;
            while ((line = reader.readLine()) != null) {
                String s = line.trim();
                if (s.isEmpty()) {
                    continue;
                }
                String[] f = s.split(",");
                if (f.length < 7) {
                    continue; // 字段不足，跳过
                }
                try {
                    double lat = Double.parseDouble(f[0].trim());
                    double lon = Double.parseDouble(f[1].trim());
                    if (lat < -90 || lat > 90 || lon < -180 || lon > 180) {
                        continue;
                    }
                    Double ele = parseFeet(f[3].trim());
                    // 日期 + 时间按 UTC 解释（与第 5 个字段"1899 年以来的天数"一致）
                    OffsetDateTime t = OffsetDateTime.parse(f[5].trim() + "T" + f[6].trim() + "Z");
                    points.add(new RawPoint(lat, lon, ele, t));
                } catch (RuntimeException ignored) {
                    // 单个坏点跳过
                }
            }
        }

        if (points.isEmpty()) {
            throw new IllegalArgumentException("文件里没有有效的轨迹点");
        }
        return new ParsedTrack(null, points);
    }

    private static Double parseFeet(String s) {
        try {
            return Double.parseDouble(s) * FEET_TO_METER;
        } catch (NumberFormatException e) {
            return null;
        }
    }
}
```

- [ ] **Step 5: 跑测试确认通过**

```powershell
& "...\mvn.cmd" -B "-Dmaven.repo.local=..." -f "...\backend\pom.xml" test "-Dtest=GeoLifeImporterTest"
```

Expected: `Tests run: 6, Failures: 0, Errors: 0`。

- [ ] **Step 6: 提交**

```bash
git add backend/src/test/resources/sample.plt backend/src/main/java/com/calcite/service/importer/GeoLifeImporter.java backend/src/test/java/com/calcite/service/importer/GeoLifeImporterTest.java
git commit -m "feat(import): GeoLife .plt 解析器（含英尺→米换算）+ 6 项测试"
```

---

## Task 6: 球面距离工具（TDD）

**Files:**
- Create: `backend/src/main/java/com/calcite/service/GeoUtils.java`
- Test: `backend/src/test/java/com/calcite/service/GeoUtilsTest.java`

- [ ] **Step 1: 写失败的测试**

创建 `backend/src/test/java/com/calcite/service/GeoUtilsTest.java`：

```java
package com.calcite.service;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

class GeoUtilsTest {

    @Test
    void 同一点距离为零() {
        assertEquals(0.0, GeoUtils.haversineMeters(25.0, 117.0, 25.0, 117.0), 1e-9);
    }

    @Test
    void 赤道上经度差一度约_111_公里() {
        double d = GeoUtils.haversineMeters(0.0, 0.0, 0.0, 1.0);
        assertEquals(111195.0, d, 50.0);
    }

    @Test
    void 真实样本首两点只差_0_078_米() {
        // 用户那份 GPX 的前两个点（间隔 1 秒）—— 起跑前站着不动，所以只挪了 8 厘米
        double d = GeoUtils.haversineMeters(25.0342562, 117.0204852, 25.0342555, 117.0204852);
        assertEquals(0.078, d, 0.005);
    }

    @Test
    void 距离是对称的() {
        double a = GeoUtils.haversineMeters(25.03, 117.02, 39.98, 116.31);
        double b = GeoUtils.haversineMeters(39.98, 116.31, 25.03, 117.02);
        assertEquals(a, b, 1e-6);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

```powershell
& "...\mvn.cmd" -B "-Dmaven.repo.local=..." -f "...\backend\pom.xml" test "-Dtest=GeoUtilsTest"
```

Expected: 编译失败，`cannot find symbol: class GeoUtils`。

- [ ] **Step 3: 实现 `GeoUtils`**

创建 `backend/src/main/java/com/calcite/service/GeoUtils.java`：

```java
package com.calcite.service;

/**
 * 地理计算小工具。
 *
 * <p>为什么在 Java 里算距离，而不是丢给 PostGIS？
 * 清洗阶段还没入库，需要用距离来判定"这段速度是否异常"，
 * 所以必须在内存里算。入库后算总距离仍然用 PostGIS（{@code ST_Length(geom::geography)}），
 * 两者公式不同会有微小差异（实测同一条轨迹差约 1.9%），这是正常的。
 */
public final class GeoUtils {

    /** 地球平均半径（米），与 PostGIS geography 用的球面模型一致 */
    private static final double EARTH_RADIUS_M = 6371008.8;

    private GeoUtils() {
    }

    /** 两点间的球面距离（米），Haversine 公式 */
    public static double haversineMeters(double lat1, double lon1, double lat2, double lon2) {
        double p1 = Math.toRadians(lat1);
        double p2 = Math.toRadians(lat2);
        double dp = p2 - p1;
        double dl = Math.toRadians(lon2 - lon1);

        double a = Math.sin(dp / 2) * Math.sin(dp / 2)
                + Math.cos(p1) * Math.cos(p2) * Math.sin(dl / 2) * Math.sin(dl / 2);
        // 浮点误差可能让 a 略大于 1，asin 会返回 NaN，所以夹一下
        return 2 * EARTH_RADIUS_M * Math.asin(Math.min(1.0, Math.sqrt(a)));
    }
}
```

- [ ] **Step 4: 跑测试确认通过**

```powershell
& "...\mvn.cmd" -B "-Dmaven.repo.local=..." -f "...\backend\pom.xml" test "-Dtest=GeoUtilsTest"
```

Expected: `Tests run: 4, Failures: 0, Errors: 0`。

- [ ] **Step 5: 提交**

```bash
git add backend/src/main/java/com/calcite/service/GeoUtils.java backend/src/test/java/com/calcite/service/GeoUtilsTest.java
git commit -m "feat(import): 球面距离工具 GeoUtils（Haversine）"
```

---

## Task 7: 清洗器（TDD，本功能的核心）

**Files:**
- Create: `backend/src/main/java/com/calcite/service/CleanedTrack.java`
- Create: `backend/src/main/java/com/calcite/service/TrackCleaner.java`
- Test: `backend/src/test/java/com/calcite/service/TrackCleanerTest.java`

- [ ] **Step 1: 先创建 `CleanedTrack`（纯数据，无逻辑）**

创建 `backend/src/main/java/com/calcite/service/CleanedTrack.java`：

```java
package com.calcite.service;

import com.calcite.service.importer.RawPoint;

import java.util.List;

/**
 * 清洗结果。三个 List 等长且与 {@code points} 一一对应。
 *
 * @param points    已按时间升序排序的点
 * @param speeds    每点的速度（米/秒）；第 0 个恒为 null（没有前一个点），
 *                  相邻时间差 &lt;= 0 时也为 null
 * @param outliers  每点是否疑似 GPS 漂移
 * @param distanceM 总距离（米）
 * @param durationS 总时长（秒）
 */
public record CleanedTrack(
        List<RawPoint> points,
        List<Double> speeds,
        List<Boolean> outliers,
        double distanceM,
        int durationS
) {
}
```

- [ ] **Step 2: 写失败的测试**

创建 `backend/src/test/java/com/calcite/service/TrackCleanerTest.java`：

```java
package com.calcite.service;

import com.calcite.service.importer.RawPoint;
import org.junit.jupiter.api.Test;

import java.time.OffsetDateTime;
import java.util.ArrayList;
import java.util.List;

import static org.junit.jupiter.api.Assertions.*;

class TrackCleanerTest {

    private static final OffsetDateTime T0 = OffsetDateTime.parse("2026-06-22T11:00:00Z");

    private final TrackCleaner cleaner = new TrackCleaner(8.0, 3.0);

    private static RawPoint p(double lat, double lon, Double ele, int secondsFromT0) {
        return new RawPoint(lat, lon, ele, T0.plusSeconds(secondsFromT0));
    }

    /** 造一条匀速直线轨迹：每点向北走约 3 米/秒 */
    private static List<RawPoint> straightLine(int count, double ele) {
        List<RawPoint> pts = new ArrayList<>();
        for (int i = 0; i < count; i++) {
            pts.add(p(25.0 + i * 0.000027, 117.0, ele, i));
        }
        return pts;
    }

    @Test
    void 按时间排序并重新编号() {
        List<RawPoint> raw = List.of(p(25.0, 117.0, 10.0, 2), p(25.0, 117.0, 10.0, 0), p(25.0, 117.0, 10.0, 1));
        CleanedTrack c = cleaner.clean(raw);

        assertEquals(T0, c.points().get(0).recordedAt());
        assertEquals(T0.plusSeconds(1), c.points().get(1).recordedAt());
        assertEquals(T0.plusSeconds(2), c.points().get(2).recordedAt());
    }

    @Test
    void 第一个点没有速度_因为没有前一个点() {
        CleanedTrack c = cleaner.clean(straightLine(5, 10.0));
        assertNull(c.speeds().get(0));
        assertNotNull(c.speeds().get(1));
    }

    @Test
    void 匀速轨迹没有任何异常点() {
        CleanedTrack c = cleaner.clean(straightLine(20, 10.0));
        assertTrue(c.outliers().stream().noneMatch(Boolean::booleanValue), "不该有异常点");
    }

    @Test
    void 海拔全相同时整条轨迹的海拔都变成_null() {
        // 用户那份数据的真实情况：2342 个点 ele 全是 0.0
        CleanedTrack c = cleaner.clean(straightLine(10, 0.0));
        assertTrue(c.points().stream().allMatch(x -> x.elevationM() == null),
                "全同的海拔应视为没有数据");
    }

    @Test
    void 海拔有变化时原样保留() {
        List<RawPoint> raw = new ArrayList<>();
        for (int i = 0; i < 5; i++) {
            raw.add(p(25.0 + i * 0.000027, 117.0, 30.0 + i, i));
        }
        CleanedTrack c = cleaner.clean(raw);
        assertEquals(30.0, c.points().get(0).elevationM(), 1e-9);
        assertEquals(34.0, c.points().get(4).elevationM(), 1e-9);
    }

    @Test
    void 单个海拔点缺失不会触发全部归_null() {
        List<RawPoint> raw = new ArrayList<>();
        raw.add(p(25.0, 117.0, 30.0, 0));
        raw.add(p(25.000027, 117.0, null, 1));
        raw.add(p(25.000054, 117.0, 32.0, 2));
        CleanedTrack c = cleaner.clean(raw);
        assertEquals(30.0, c.points().get(0).elevationM(), 1e-9);
        assertNull(c.points().get(1).elevationM());
    }

    @Test
    void 慢速轨迹里的跳点被标成异常_且两端都标() {
        // 前 10 个点约 3 米/秒，第 11 个点突然跳到 1 公里外
        List<RawPoint> raw = new ArrayList<>(straightLine(10, 10.0));
        raw.add(p(25.0, 117.0, 10.0, 10));      // 瞬间跳回起点附近 → 造成两段超速
        raw.add(p(25.000270, 117.0, 10.0, 11));

        CleanedTrack c = cleaner.clean(raw);
        long outlierCount = c.outliers().stream().filter(Boolean::booleanValue).count();
        assertTrue(outlierCount >= 2, "跳点和它相邻的点都该被标记，实际标了 " + outlierCount);
    }

    @Test
    void 快速轨迹_高绝对速度不会被误标() {
        // 模拟 GeoLife 汽车轨迹：每点约 20 米/秒（72 km/h），中位数也是 20
        // 阈值 = max(8, 3 × 20) = 60，所以 20 不该被标
        List<RawPoint> raw = new ArrayList<>();
        for (int i = 0; i < 30; i++) {
            raw.add(p(25.0 + i * 0.00018, 117.0, 10.0, i));
        }
        CleanedTrack c = cleaner.clean(raw);
        assertTrue(c.outliers().stream().noneMatch(Boolean::booleanValue),
                "高速但平稳的轨迹不该被标记");
    }

    @Test
    void 时间重复的点速度存_null() {
        List<RawPoint> raw = List.of(p(25.0, 117.0, 10.0, 0), p(25.000027, 117.0, 10.0, 0));
        CleanedTrack c = cleaner.clean(raw);
        assertNull(c.speeds().get(1), "时间差为 0 时速度无法计算，应为 null");
    }

    @Test
    void 总距离和总时长() {
        CleanedTrack c = cleaner.clean(straightLine(11, 10.0));
        assertEquals(10, c.durationS());
        assertTrue(c.distanceM() > 20 && c.distanceM() < 40,
                "10 段 × 约 3 米 ≈ 30 米，实际 " + c.distanceM());
    }
}
```

- [ ] **Step 3: 跑测试确认失败**

```powershell
& "...\mvn.cmd" -B "-Dmaven.repo.local=..." -f "...\backend\pom.xml" test "-Dtest=TrackCleanerTest"
```

Expected: 编译失败，`cannot find symbol: class TrackCleaner`。

- [ ] **Step 4: 实现 `TrackCleaner`**

创建 `backend/src/main/java/com/calcite/service/TrackCleaner.java`：

```java
package com.calcite.service;

import com.calcite.service.importer.RawPoint;

import java.time.Duration;
import java.util.ArrayList;
import java.util.Comparator;
import java.util.List;

/**
 * 轨迹清洗 —— **所有清洗规则只写在这一处**。
 *
 * <p>规则（详见设计文档第六节）：
 * <ol>
 *   <li>按时间升序排序</li>
 *   <li>算每点速度（第 0 点、时间差 &lt;= 0 的点为 null）</li>
 *   <li>海拔整条全同 → 全部置 null（"没记录"而不是"海拔 0"）</li>
 *   <li>速度超阈值 → 把该段**两端**都标记为疑似漂移</li>
 * </ol>
 *
 * <p>阈值是**自适应**的：{@code max(绝对下限, 倍数 × 该轨迹速度中位数)}。
 * 因为 GeoLife 里有汽车（20 m/s）甚至火车（80 m/s）轨迹，固定阈值会把它们全标成异常。
 */
public class TrackCleaner {

    private final double maxSpeedMps;
    private final double speedMedianFactor;

    public TrackCleaner(double maxSpeedMps, double speedMedianFactor) {
        this.maxSpeedMps = maxSpeedMps;
        this.speedMedianFactor = speedMedianFactor;
    }

    public CleanedTrack clean(List<RawPoint> raw) {
        if (raw == null || raw.size() < 2) {
            throw new IllegalArgumentException("至少需要 2 个轨迹点");
        }

        // ① 排序
        List<RawPoint> pts = new ArrayList<>(raw);
        pts.sort(Comparator.comparing(RawPoint::recordedAt));
        int n = pts.size();

        // ② 每点速度（第 i 点 = 从 i-1 到 i 的速度）
        List<Double> speeds = new ArrayList<>(n);
        double totalDistance = 0;
        for (int i = 0; i < n; i++) {
            if (i == 0) {
                speeds.add(null);
                continue;
            }
            RawPoint a = pts.get(i - 1);
            RawPoint b = pts.get(i);
            double d = GeoUtils.haversineMeters(a.lat(), a.lon(), b.lat(), b.lon());
            totalDistance += d;
            double dt = Duration.between(a.recordedAt(), b.recordedAt()).toMillis() / 1000.0;
            speeds.add(dt > 0 ? d / dt : null);
        }

        // ③ 自适应阈值 + 标记
        double threshold = thresholdFor(speeds);
        List<Boolean> outliers = new ArrayList<>(n);
        for (int i = 0; i < n; i++) {
            outliers.add(false);
        }
        for (int i = 1; i < n; i++) {
            Double v = speeds.get(i);
            if (v != null && v > threshold) {
                // 无法判断是段的哪一端漂了，两端都标（宁可多标不可漏标）
                outliers.set(i - 1, true);
                outliers.set(i, true);
            }
        }

        // ④ 海拔整条全同 → 全置 null
        List<RawPoint> fixed = normalizeElevation(pts);

        int durationS = (int) Duration.between(pts.get(0).recordedAt(), pts.get(n - 1).recordedAt()).toSeconds();
        return new CleanedTrack(fixed, speeds, outliers, totalDistance, durationS);
    }

    /** 阈值 = max(绝对下限, 倍数 × 中位数)；没有任何有效速度时只用绝对下限 */
    private double thresholdFor(List<Double> speeds) {
        List<Double> valid = new ArrayList<>();
        for (Double v : speeds) {
            if (v != null && v > 0) {
                valid.add(v);
            }
        }
        if (valid.isEmpty()) {
            return maxSpeedMps;
        }
        valid.sort(Comparator.naturalOrder());
        int m = valid.size();
        double median = (m % 2 == 1)
                ? valid.get(m / 2)
                : (valid.get(m / 2 - 1) + valid.get(m / 2)) / 2.0;
        return Math.max(maxSpeedMps, speedMedianFactor * median);
    }

    /**
     * 海拔全同（含全部为 null）时，一律当成"没有海拔数据"。
     *
     * <p>实测样本 ele 全是 0.0 —— 存 0 会在图上画出一条贴底直线，
     * 而福建校园实际海拔约 300 米，0 表示"没记录"。前端已有"没有海拔数据"的降级显示。
     */
    private static List<RawPoint> normalizeElevation(List<RawPoint> pts) {
        Double first = pts.get(0).elevationM();
        boolean allSame = true;
        for (RawPoint p : pts) {
            Double e = p.elevationM();
            if (first == null ? e != null : !first.equals(e)) {
                allSame = false;
                break;
            }
        }
        if (!allSame) {
            return pts;
        }
        List<RawPoint> out = new ArrayList<>(pts.size());
        for (RawPoint p : pts) {
            out.add(new RawPoint(p.lat(), p.lon(), null, p.recordedAt()));
        }
        return out;
    }
}
```

- [ ] **Step 5: 跑测试确认通过**

```powershell
& "...\mvn.cmd" -B "-Dmaven.repo.local=..." -f "...\backend\pom.xml" test "-Dtest=TrackCleanerTest"
```

Expected: `Tests run: 10, Failures: 0, Errors: 0`。

- [ ] **Step 6: 提交**

```bash
git add backend/src/main/java/com/calcite/service/CleanedTrack.java backend/src/main/java/com/calcite/service/TrackCleaner.java backend/src/test/java/com/calcite/service/TrackCleanerTest.java
git commit -m "feat(import): 清洗器 —— 排序/速度/自适应阈值标记/海拔归 NULL + 10 项测试"
```

---

## Task 8: 编排服务 `ImportService`

**Files:**
- Create: `backend/src/main/java/com/calcite/web/dto/ImportResult.java`
- Create: `backend/src/main/java/com/calcite/service/ImportService.java`
- Test: `backend/src/test/java/com/calcite/service/ImportServiceTest.java`

- [ ] **Step 1: 创建 `ImportResult` DTO**

创建 `backend/src/main/java/com/calcite/web/dto/ImportResult.java`：

```java
package com.calcite.web.dto;

/**
 * 导入结果 —— 前端拿它显示摘要。
 *
 * @param id                轨迹主键
 * @param name              轨迹名
 * @param pointCount        点数
 * @param distanceM         总距离（米，Java 按球面公式算）
 * @param durationS         总时长（秒）
 * @param outlierCount      疑似 GPS 漂移点数
 * @param skippedDuplicate  是否是"已导入过"（true 时没有新建轨迹）
 * @param message           给人看的一句话
 */
public record ImportResult(
        Long id,
        String name,
        int pointCount,
        double distanceM,
        int durationS,
        long outlierCount,
        boolean skippedDuplicate,
        String message
) {
}
```

- [ ] **Step 2: 写失败的测试（幂等是重点）**

创建 `backend/src/test/java/com/calcite/service/ImportServiceTest.java`：

```java
package com.calcite.service;

import com.calcite.domain.Track;
import com.calcite.repository.TrackPointRepository;
import com.calcite.repository.TrackRepository;
import com.calcite.web.dto.ImportResult;
import org.junit.jupiter.api.Test;

import java.nio.charset.StandardCharsets;
import java.util.Optional;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.ArgumentMatchers.anyString;
import static org.mockito.Mockito.*;

class ImportServiceTest {

    /** 造一个最小可用的 GPX（3 个点，向北匀速） */
    private static byte[] smallGpx() {
        StringBuilder sb = new StringBuilder("<?xml version='1.0' encoding='UTF-8'?><gpx><trk><trkseg>");
        for (int i = 0; i < 3; i++) {
            sb.append("<trkpt lat=\"").append(25.0 + i * 0.000027).append("\" lon=\"117.0\">")
              .append("<ele>30.0</ele>")
              .append("<time>2026-06-22T11:00:0").append(i).append("Z</time>")
              .append("</trkpt>");
        }
        return sb.append("</trkseg></trk></gpx>").getBytes(StandardCharsets.UTF_8);
    }

    private ImportService newService(TrackRepository tracks, TrackPointRepository points) {
        return new ImportService(tracks, points,
                new TrackCleaner(8.0, 3.0),
                new com.calcite.service.importer.GpxImporter(),
                new com.calcite.service.importer.GeoLifeImporter(),
                // 这几个测试只测 importBytes（单文件路径），批量导入的事务模板用不到，
                // 给个 mock 就够了
                mock(org.springframework.transaction.PlatformTransactionManager.class));
    }

    @Test
    void 导入新文件_保存轨迹和点() {
        TrackRepository tracks = mock(TrackRepository.class);
        TrackPointRepository points = mock(TrackPointRepository.class);
        when(tracks.findByExternalId(anyString())).thenReturn(Optional.empty());
        when(tracks.save(any(Track.class))).thenAnswer(inv -> inv.getArgument(0));

        ImportResult r = newService(tracks, points).importBytes(smallGpx(), "晨跑");

        assertEquals(3, r.pointCount());
        assertFalse(r.skippedDuplicate());
        verify(tracks, times(1)).save(any(Track.class));
        verify(points, times(1)).saveAll(any());
    }

    @Test
    void 重复导入同一内容_不新建轨迹() {
        TrackRepository tracks = mock(TrackRepository.class);
        TrackPointRepository points = mock(TrackPointRepository.class);

        Track existing = new Track("老轨迹", "gpx", "x", null, null);
        when(tracks.findByExternalId(anyString())).thenReturn(Optional.of(existing));

        ImportResult r = newService(tracks, points).importBytes(smallGpx(), "晨跑");

        assertTrue(r.skippedDuplicate());
        assertEquals("这条轨迹已经导入过了", r.message());
        verify(tracks, never()).save(any(Track.class));
    }

    @Test
    void 同一份内容两次调用_external_id_相同() {
        TrackRepository tracks = mock(TrackRepository.class);
        TrackPointRepository points = mock(TrackPointRepository.class);
        when(tracks.findByExternalId(anyString())).thenReturn(Optional.empty());
        when(tracks.save(any(Track.class))).thenAnswer(inv -> inv.getArgument(0));

        ImportService svc = newService(tracks, points);
        svc.importBytes(smallGpx(), "a");
        svc.importBytes(smallGpx(), "b");

        // 两次都应该用同一个 externalId 去查重
        verify(tracks, times(2)).findByExternalId(anyString());
    }

    @Test
    void 无法识别的格式抛异常() {
        TrackRepository tracks = mock(TrackRepository.class);
        TrackPointRepository points = mock(TrackPointRepository.class);
        ImportService svc = newService(tracks, points);

        assertThrows(IllegalArgumentException.class,
                () -> svc.importBytes("这不是轨迹".getBytes(StandardCharsets.UTF_8), "x"));
    }

    @Test
    void 名字缺失时用兜底名() {
        TrackRepository tracks = mock(TrackRepository.class);
        TrackPointRepository points = mock(TrackPointRepository.class);
        when(tracks.findByExternalId(anyString())).thenReturn(Optional.empty());
        when(tracks.save(any(Track.class))).thenAnswer(inv -> inv.getArgument(0));

        ImportResult r = newService(tracks, points).importBytes(smallGpx(), "2026年6月22日户外跑步");
        assertEquals("2026年6月22日户外跑步", r.name());
    }
}
```

- [ ] **Step 3: 跑测试确认失败**

```powershell
& "...\mvn.cmd" -B "-Dmaven.repo.local=..." -f "...\backend\pom.xml" test "-Dtest=ImportServiceTest"
```

Expected: 编译失败，`cannot find symbol: class ImportService`。

- [ ] **Step 4: 实现 `ImportService`**

创建 `backend/src/main/java/com/calcite/service/ImportService.java`：

```java
package com.calcite.service;

import com.calcite.domain.Track;
import com.calcite.domain.TrackPoint;
import com.calcite.repository.TrackPointRepository;
import com.calcite.repository.TrackRepository;
import com.calcite.service.importer.FormatDetector;
import com.calcite.service.importer.GeoLifeImporter;
import com.calcite.service.importer.GpxImporter;
import com.calcite.service.importer.Importer;
import com.calcite.service.importer.ParsedTrack;
import com.calcite.service.importer.RawPoint;
import com.calcite.web.dto.ImportResult;
import org.locationtech.jts.geom.Coordinate;
import org.locationtech.jts.geom.GeometryFactory;
import org.locationtech.jts.geom.LineString;
import org.locationtech.jts.geom.Point;
import org.locationtech.jts.geom.PrecisionModel;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.io.ByteArrayInputStream;
import java.nio.charset.StandardCharsets;
import java.security.MessageDigest;
import java.time.OffsetDateTime;
import java.util.ArrayList;
import java.util.List;
import java.util.Optional;

/**
 * 导入编排：识别格式 → 解析 → 清洗 → 入库。
 *
 * <p>两个入口（网页上传单文件、目录批量）共用这一套流程。
 */
@Service
public class ImportService {

    /** WGS84。JTS 默认 SRID 是 0，必须显式指定，否则 PostGIS 会拒绝写入 */
    private static final int SRID = 4326;
    private static final GeometryFactory GEOMETRY_FACTORY = new GeometryFactory(new PrecisionModel(), SRID);
    /** 判定格式时只看开头这么多字节 */
    private static final int HEAD_BYTES = 4096;

    private final TrackRepository trackRepository;
    private final TrackPointRepository trackPointRepository;
    private final TrackCleaner cleaner;
    private final GpxImporter gpxImporter;
    private final GeoLifeImporter geoLifeImporter;

    public ImportService(TrackRepository trackRepository,
                         TrackPointRepository trackPointRepository,
                         TrackCleaner cleaner,
                         GpxImporter gpxImporter,
                         GeoLifeImporter geoLifeImporter) {
        this.trackRepository = trackRepository;
        this.trackPointRepository = trackPointRepository;
        this.cleaner = cleaner;
        this.gpxImporter = gpxImporter;
        this.geoLifeImporter = geoLifeImporter;
    }

    /**
     * 从文件字节导入一条轨迹。
     *
     * @param fallbackName 文件里没有轨迹名时用它（通常传文件名去掉扩展名）
     */
    @Transactional
    public ImportResult importBytes(byte[] content, String fallbackName) {
        if (content == null || content.length == 0) {
            throw new IllegalArgumentException("上传的文件是空的");
        }

        String externalId = sha256Hex(content);
        Optional<Track> existing = trackRepository.findByExternalId(externalId);
        if (existing.isPresent()) {
            Track t = existing.get();
            return new ImportResult(t.getId(), t.getName(),
                    t.getPointCount() == null ? 0 : t.getPointCount(),
                    t.getDistanceM() == null ? 0 : t.getDistanceM(),
                    t.getDurationS() == null ? 0 : t.getDurationS(),
                    0, true, "这条轨迹已经导入过了");
        }

        Importer importer = pickImporter(content);
        ParsedTrack parsed;
        try {
            parsed = importer.parse(new ByteArrayInputStream(content));
        } catch (IllegalArgumentException e) {
            throw e;
        } catch (Exception e) {
            throw new IllegalArgumentException("文件内容不是合法的 " + importer.format() + "：" + e.getMessage(), e);
        }

        String name = (parsed.name() == null || parsed.name().isBlank())
                ? (fallbackName == null || fallbackName.isBlank() ? "未命名轨迹" : fallbackName)
                : parsed.name();

        return persist(name, importer.format(), externalId, parsed.points());
    }

    /** 按内容选解析器 */
    private Importer pickImporter(byte[] content) {
        byte[] head = new byte[Math.min(HEAD_BYTES, content.length)];
        System.arraycopy(content, 0, head, 0, head.length);
        String format = FormatDetector.detect(head);
        return switch (format) {
            case "gpx" -> gpxImporter;
            case "geolife" -> geoLifeImporter;
            default -> throw new IllegalArgumentException("无法识别的文件格式（只支持 GPX 和 GeoLife .plt）");
        };
    }

    /** 清洗 + 入库，返回给前端的摘要 */
    private ImportResult persist(String name, String source, String externalId, List<RawPoint> raw) {
        CleanedTrack cleaned = cleaner.clean(raw);
        List<RawPoint> pts = cleaned.points();
        int n = pts.size();

        OffsetDateTime start = pts.get(0).recordedAt();
        OffsetDateTime end = pts.get(n - 1).recordedAt();

        Track track = new Track(name, source, externalId, start, end);
        track.setDistanceM(cleaned.distanceM());
        track.setDurationS(cleaned.durationS());
        track.setPointCount(n);
        track.setGeom(buildLineString(pts));
        Track saved = trackRepository.save(track);

        List<TrackPoint> entities = new ArrayList<>(n);
        for (int i = 0; i < n; i++) {
            RawPoint p = pts.get(i);
            entities.add(new TrackPoint(
                    saved.getId(),
                    i,
                    p.recordedAt(),
                    p.elevationM(),
                    cleaned.speeds().get(i),
                    buildPoint(p),
                    cleaned.outliers().get(i)));
        }
        trackPointRepository.saveAll(entities);

        long outlierCount = cleaned.outliers().stream().filter(Boolean::booleanValue).count();
        return new ImportResult(saved.getId(), saved.getName(), n, cleaned.distanceM(),
                cleaned.durationS(), outlierCount, false, "导入成功");
    }

    private static LineString buildLineString(List<RawPoint> pts) {
        Coordinate[] coords = new Coordinate[pts.size()];
        for (int i = 0; i < pts.size(); i++) {
            // JTS 的 Coordinate 是 (x, y) = (经度, 纬度)，别写反
            coords[i] = new Coordinate(pts.get(i).lon(), pts.get(i).lat());
        }
        return GEOMETRY_FACTORY.createLineString(coords);
    }

    private static Point buildPoint(RawPoint p) {
        return GEOMETRY_FACTORY.createPoint(new Coordinate(p.lon(), p.lat()));
    }

    /** 文件内容的 SHA-256 十六进制（取前 32 位够用），用来判重——改文件名也认得出来 */
    static String sha256Hex(byte[] content) {
        try {
            MessageDigest md = MessageDigest.getInstance("SHA-256");
            byte[] digest = md.digest(content);
            StringBuilder sb = new StringBuilder();
            for (int i = 0; i < 16; i++) {
                sb.append(String.format("%02x", digest[i]));
            }
            return sb.toString();
        } catch (Exception e) {
            throw new IllegalStateException("算哈希失败", e);
        }
    }
}
```

- [ ] **Step 5: 跑测试确认通过**

```powershell
& "...\mvn.cmd" -B "-Dmaven.repo.local=..." -f "...\backend\pom.xml" test "-Dtest=ImportServiceTest"
```

Expected: `Tests run: 5, Failures: 0, Errors: 0`。

- [ ] **Step 6: 把 `TrackCleaner` 注册成 Bean**

`ImportService` 的构造器需要 `TrackCleaner`，但它现在只是个普通类。在 `TrackCleaner` 上加两处：

```java
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

@Component
public class TrackCleaner {

    private final double maxSpeedMps;
    private final double speedMedianFactor;

    public TrackCleaner(@Value("${calcite.import.max-speed-mps:8.0}") double maxSpeedMps,
                        @Value("${calcite.import.speed-median-factor:3}") double speedMedianFactor) {
        this.maxSpeedMps = maxSpeedMps;
        this.speedMedianFactor = speedMedianFactor;
    }
    // ... 其余不变
```

`GpxImporter` / `GeoLifeImporter` 也要能注入，各自加 `@Component`：

```java
import org.springframework.stereotype.Component;

@Component
public class GpxImporter implements Importer {
```

```java
import org.springframework.stereotype.Component;

@Component
public class GeoLifeImporter implements Importer {
```

- [ ] **Step 7: 编译 + 跑全部后端测试**

```powershell
& "...\mvn.cmd" -B "-Dmaven.repo.local=..." -f "...\backend\pom.xml" test
```

Expected: 全部通过（`Tests run: 34` 左右），`BUILD SUCCESS`。

- [ ] **Step 8: 提交**

```bash
git add backend/src/main/java/com/calcite/service/ImportService.java backend/src/main/java/com/calcite/web/dto/ImportResult.java backend/src/test/java/com/calcite/service/ImportServiceTest.java backend/src/main/java/com/calcite/service/TrackCleaner.java backend/src/main/java/com/calcite/service/importer/GpxImporter.java backend/src/main/java/com/calcite/service/importer/GeoLifeImporter.java
git commit -m "feat(import): ImportService 编排 + 内容哈希幂等 + 5 项测试"
```

---

## Task 9: Web 层（两个接口 + 配置）

**Files:**
- Create: `backend/src/main/java/com/calcite/web/ImportController.java`
- Modify: `backend/src/main/resources/application.yml`
- Modify: `backend/src/main/java/com/calcite/web/dto/TrackPointDto.java`
- Modify: `backend/src/main/java/com/calcite/service/ImportService.java`（追加目录批量方法）

- [ ] **Step 1: 配置**

打开 `backend/src/main/resources/application.yml`，在 `spring:` 块里补批量插入参数（`jpa:` 之下、`properties:` 同级追加）：

```yaml
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        # 批量插入：百万点级导入靠它，否则逐条 insert 会非常慢
        jdbc:
          batch_size: 500
        order_inserts: true
        order_updates: true
```

并把数据源 url 改成带批量重写参数的版本：

```yaml
  datasource:
    url: jdbc:postgresql://localhost:5432/calcite?reWriteBatchedInserts=true
```

文件**末尾**追加导入配置：

```yaml
# 轨迹导入
calcite:
  import:
    # 允许批量导入的根目录白名单。接口收到的 path 必须落在其中之一，
    # 否则拒绝 —— 本地项目不需要登录，但路径必须收敛，避免任意目录读取。
    allowed-roots:
      - D:\GeoLife
    # 单次批量导入最多处理多少条轨迹（配合幂等，脚本循环调用即可自动推进）
    max-tracks-per-call: 50
    # 异常速度判定的绝对下限（米/秒）
    max-speed-mps: 8.0
    # 相对倍数：阈值 = max(max-speed-mps, 倍数 × 该轨迹速度中位数)
    speed-median-factor: 3
```

- [ ] **Step 2: `ImportService` 追加目录批量导入方法**

⚠️ **这里故意不加 `@Transactional`。**

**为什么**：`importGeoLifeDirectory` 内部要调用同一个类的 `importBytes`，而 Spring 的 `@Transactional` 靠**代理**生效——**类内部自己调自己不走代理**，内层事务不会开。

如果外层标上 `@Transactional`，整批就成了一个巨型事务：**一条坏数据把事务标记成 "rollback-only" 之后，后面所有文件的保存都会失败**，`catch` 也救不回来（这是 Spring 里非常经典的坑）。

**做法**：外层不标事务，改成**每个文件用 `TransactionTemplate` 单独开一个事务**。单个文件失败只回滚它自己，`catch` 能正常继续处理下一个。

先给 `ImportService` 加一个字段，构造器多收一个参数：

```java
    private final org.springframework.transaction.support.TransactionTemplate txTemplate;

    public ImportService(TrackRepository trackRepository,
                         TrackPointRepository trackPointRepository,
                         TrackCleaner cleaner,
                         GpxImporter gpxImporter,
                         GeoLifeImporter geoLifeImporter,
                         org.springframework.transaction.PlatformTransactionManager txManager) {
        this.trackRepository = trackRepository;
        this.trackPointRepository = trackPointRepository;
        this.cleaner = cleaner;
        this.gpxImporter = gpxImporter;
        this.geoLifeImporter = geoLifeImporter;
        this.txTemplate = new org.springframework.transaction.support.TransactionTemplate(txManager);
    }
```

然后追加批量导入方法（放在 `sha256Hex` 之前）：

```java
    /**
     * 批量导入一个 GeoLife 数据目录。
     *
     * <p>每调用一次最多处理 {@code maxTracks} 条；因为幂等，重复调用会自动跳过已导入的、
     * 继续往后取 —— 脚本循环调用即可分批灌完全部数据。
     *
     * <p><b>故意不加 {@code @Transactional}</b>：见本步骤开头的说明。
     * 每个文件走 {@code txTemplate} 单独开事务，单条失败不拖垮整批。
     */
    public GeoLifeImportResult importGeoLifeDirectory(java.nio.file.Path root, int maxTracks,
                                                      java.util.List<String> allowedRoots) {
        if (root == null || !java.nio.file.Files.isDirectory(root)) {
            throw new IllegalArgumentException("目录不存在：" + root);
        }
        if (!isUnderAllowedRoot(root, allowedRoots)) {
            throw new IllegalArgumentException("路径不在允许的导入目录内，允许：" + allowedRoots);
        }

        java.util.List<java.nio.file.Path> pltFiles;
        try (var stream = java.nio.file.Files.walk(root)) {
            pltFiles = stream.filter(java.nio.file.Files::isRegularFile)
                    .filter(p -> p.getFileName().toString().toLowerCase().endsWith(".plt"))
                    .sorted()
                    .toList();
        } catch (java.io.IOException e) {
            throw new IllegalArgumentException("扫描目录失败：" + e.getMessage(), e);
        }

        int imported = 0;
        int skipped = 0;
        int failed = 0;
        int totalPoints = 0;
        long started = System.currentTimeMillis();

        for (java.nio.file.Path file : pltFiles) {
            if (imported >= maxTracks) {
                skipped++;
                continue;
            }
            try {
                byte[] content = java.nio.file.Files.readAllBytes(file);
                String fallback = file.getFileName().toString().replaceAll("\\.plt$", "");
                ImportResult r = txTemplate.execute(status -> importBytes(content, fallback));
                if (r.skippedDuplicate()) {
                    skipped++;
                } else {
                    imported++;
                    totalPoints += r.pointCount();
                }
            } catch (Exception e) {
                failed++; // 单条失败不中断整批
            }
        }

        return new GeoLifeImportResult(root.toString(), pltFiles.size(), imported, skipped, failed,
                totalPoints, System.currentTimeMillis() - started);
    }

    private static boolean isUnderAllowedRoot(java.nio.file.Path path, java.util.List<String> allowedRoots) {
        if (allowedRoots == null || allowedRoots.isEmpty()) {
            return false;
        }
        java.nio.file.Path real = path.toAbsolutePath().normalize();
        for (String r : allowedRoots) {
            java.nio.file.Path allowed = java.nio.file.Path.of(r).toAbsolutePath().normalize();
            if (real.startsWith(allowed)) {
                return true;
            }
        }
        return false;
    }
```

并在 `ImportService.java` **末尾**（最后一个 `}` 之前）追加结果 record：

```java
    /** 批量导入的汇总结果 */
    public record GeoLifeImportResult(
            String path, int scanned, int imported, int skipped, int failed,
            int totalPoints, long elapsedMs) {
    }
```

- [ ] **Step 3: `TrackPointDto` 暴露 `isOutlier`**

改 `backend/src/main/java/com/calcite/web/dto/TrackPointDto.java`：在 record 组件里加一个字段，并在 `from` 里传值。

把 record 声明改成：

```java
public record TrackPointDto(
        Integer seq,
        OffsetDateTime recordedAt,
        double lon,
        double lat,
        Double elevationM,
        Double speedMps,
        boolean outlier
) {
```

并在 `from` 方法的 `new TrackPointDto(...)` 参数列表**末尾**加 `point.isOutlier()`：

```java
                point.getSpeedMps(),
                point.isOutlier()
        );
```

- [ ] **Step 4: 建配置绑定类 `ImportProperties`**

⚠️ 注意：配置里 `allowed-roots` 是 YAML **列表**，**不能用 `@Value` 绑定**（`@Value` 只能读单个标量属性，YAML 列表在 Spring 里是 `xxx[0]`、`xxx[1]` 形式，`@Value("${calcite.import.allowed-roots}")` 会直接报占位符解析失败）。列表必须用 `@ConfigurationProperties`。

创建 `backend/src/main/java/com/calcite/config/ImportProperties.java`：

```java
package com.calcite.config;

import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.stereotype.Component;

import java.util.ArrayList;
import java.util.List;

/** 轨迹导入配置，对应 application.yml 的 calcite.import.* */
@Component
@ConfigurationProperties(prefix = "calcite.import")
public class ImportProperties {

    /** 允许批量导入的根目录白名单 */
    private List<String> allowedRoots = new ArrayList<>();

    /** 单次批量导入最多处理多少条轨迹 */
    private int maxTracksPerCall = 50;

    // Spring 绑定需要 setter（getter 给业务代码用）
    public List<String> getAllowedRoots() {
        return allowedRoots;
    }

    public void setAllowedRoots(List<String> allowedRoots) {
        this.allowedRoots = allowedRoots;
    }

    public int getMaxTracksPerCall() {
        return maxTracksPerCall;
    }

    public void setMaxTracksPerCall(int maxTracksPerCall) {
        this.maxTracksPerCall = maxTracksPerCall;
    }
}
```

（`max-speed-mps` 和 `speed-median-factor` 两个标量仍由 `TrackCleaner` 用 `@Value` 注入——标量用 `@Value` 没问题，而且那样测试可以直接 `new TrackCleaner(8.0, 3.0)`，不必启动 Spring。）

- [ ] **Step 5: 建 `ImportController`**

创建 `backend/src/main/java/com/calcite/web/ImportController.java`：

```java
package com.calcite.web;

import com.calcite.config.ImportProperties;
import com.calcite.service.ImportService;
import com.calcite.web.dto.ImportResult;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.bind.annotation.ResponseStatus;
import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.multipart.MultipartFile;

import java.io.IOException;
import java.nio.file.Path;
import java.util.Map;

/**
 * 轨迹导入接口。
 *
 * <p>两个入口：
 * <ul>
 *   <li>{@code POST /api/tracks/import} —— 网页上传单个文件（GPX）</li>
 *   <li>{@code POST /api/import/geolife} —— 批量导入本地目录（GeoLife .plt）</li>
 * </ul>
 */
@RestController
public class ImportController {

    private final ImportService importService;
    private final ImportProperties properties;

    public ImportController(ImportService importService, ImportProperties properties) {
        this.importService = importService;
        this.properties = properties;
    }

    /** 网页上传单个文件 */
    @PostMapping("/api/tracks/import")
    public ImportResult upload(@RequestParam("file") MultipartFile file) throws IOException {
        if (file == null || file.isEmpty()) {
            throw new IllegalArgumentException("上传的文件是空的");
        }
        return importService.importBytes(file.getBytes(), stripExtension(file.getOriginalFilename()));
    }

    /** 批量导入本地 GeoLife 目录 */
    @PostMapping("/api/import/geolife")
    public ImportService.GeoLifeImportResult importGeoLife(@RequestBody GeoLifeRequest request) {
        if (request.path() == null || request.path().isBlank()) {
            throw new IllegalArgumentException("必须提供 path");
        }
        int max = request.maxTracks() == null ? properties.getMaxTracksPerCall() : request.maxTracks();
        return importService.importGeoLifeDirectory(
                Path.of(request.path()), max, properties.getAllowedRoots());
    }

    /** 请求体：{"path": "D:\\GeoLife\\Data", "maxTracks": 50} */
    public record GeoLifeRequest(String path, Integer maxTracks) {
    }

    /** 文件名去掉扩展名，用作轨迹名兜底 */
    private static String stripExtension(String filename) {
        if (filename == null || filename.isBlank()) {
            return "未命名轨迹";
        }
        int dot = filename.lastIndexOf('.');
        return dot > 0 ? filename.substring(0, dot) : filename;
    }

    /** 参数问题一律 400，并把原因原样告诉前端（这些错误都是给人看的） */
    @ExceptionHandler(IllegalArgumentException.class)
    @ResponseStatus(HttpStatus.BAD_REQUEST)
    public Map<String, String> handleBadRequest(IllegalArgumentException e) {
        return Map.of("error", e.getMessage() == null ? "导入失败" : e.getMessage());
    }
}
```

- [ ] **Step 6: 编译并跑全部后端测试**

```powershell
& "...\mvn.cmd" -B "-Dmaven.repo.local=..." -f "...\backend\pom.xml" test
```

Expected: `BUILD SUCCESS`。

- [ ] **Step 7: 启动后端手工验证（真实数据）**

```powershell
# 后台启动后端后，用真实文件打接口
curl.exe -s -X POST http://localhost:8080/api/tracks/import -F "file=@D:\Calcite-note\GPX-Data\2026年6月22日户外跑步.gpx.bin_tmp"
```

Expected：返回 JSON，`pointCount` = **2342**，`outlierCount` = **8**，`skippedDuplicate` = `false`。

> **`outlierCount = 8` 是精确预期值**，不是"大概"。已用真实数据代入算法算过：
> 速度中位数 1.627 m/s → 3×中位数 = 4.88 → 阈值 = `max(8, 4.88)` = **8.00 m/s**；
> 超阈值的段有 4 段（速度 8.97 / 10.87 / 11.28 / 12.13 m/s），两端都标 → **正好 8 个点**。
> 如果算出来不是 8，说明实现有偏差，要查。

再打一次同样的上传 → `skippedDuplicate` 应为 `true`。

用 psql 核对（**精确断言，包含被标点的 seq**）：

```powershell
$env:PGPASSWORD='见 application-local.yml'; $env:LC_MESSAGES='C'
& "E:\PostgreSQL\bin\psql.exe" -U postgres -d calcite -P pager=off -c "SELECT count(*) AS 点数, count(*) FILTER (WHERE is_outlier) AS 异常点, count(*) FILTER (WHERE elevation_m IS NULL) AS 海拔空 FROM track_point WHERE track_id = (SELECT max(id) FROM track);"
& "E:\PostgreSQL\bin\psql.exe" -U postgres -d calcite -P pager=off -c "SELECT seq FROM track_point WHERE track_id = (SELECT max(id) FROM track) AND is_outlier ORDER BY seq;"
```

Expected：
- 第一句：`点数 = 2342`、`异常点 = 8`、`海拔空 = 2342`（海拔全部归 NULL）
- 第二句：seq 依次为 `1128, 1129, 1135, 1136, 1144, 1145, 1585, 1586`

- [ ] **Step 8: 提交**

```bash
git add backend/src/main/java/com/calcite/web/ImportController.java backend/src/main/java/com/calcite/config/ImportProperties.java backend/src/main/resources/application.yml backend/src/main/java/com/calcite/web/dto/TrackPointDto.java backend/src/main/java/com/calcite/service/ImportService.java
git commit -m "feat(import): 上传接口 + GeoLife 目录批量接口 + 导入配置"
```

---

## Task 10: 前端上传入口

**Files:**
- Modify: `frontend/src/components/TrackList.vue`
- Modify: `frontend/src/App.vue`

- [ ] **Step 1: `TrackList.vue` 加按钮和事件**

在 `<script setup>` 里，把 `defineEmits` 那一行改成：

```js
// 告诉父组件"用户点了哪条" / "用户选了要导入的文件"
const emit = defineEmits(['select', 'import'])

/** 用户选完文件 → 上报给父组件（组件自己不发请求） */
function onFilePicked(ev) {
  const file = ev.target.files?.[0]
  if (file) emit('import', file)
  // 清空 input，否则连续选同一个文件不会触发 change
  ev.target.value = ''
}
```

在 `<template>` 里，把 `<div class="track-list">` 的开头改成：

```html
  <div class="track-list">
    <div class="import-bar">
      <label class="import-btn">
        导入轨迹
        <input type="file" accept=".gpx,.plt" hidden @change="onFilePicked" data-testid="import-input" />
      </label>
      <span class="import-hint">支持 GPX / GeoLife .plt</span>
    </div>

    <p v-if="loading" class="hint">加载中…</p>
```

在 `<style scoped>` 末尾追加：

```css
.import-bar {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 8px;
}

.import-btn {
  padding: 5px 12px;
  border: 1px solid rgba(127, 209, 255, 0.45);
  border-radius: 7px;
  background: rgba(127, 209, 255, 0.12);
  font-size: 12px;
  cursor: pointer;
  transition: background 0.15s;
}

.import-btn:hover {
  background: rgba(127, 209, 255, 0.25);
}

.import-hint {
  font-size: 11px;
  color: #93a4bb;
}
```

- [ ] **Step 2: `App.vue` 加上传逻辑**

在 `<script setup>` 里，`loadTracks` 函数之后追加：

```js
/* ============ 导入 ============ */
const importing = ref(false)
const importMessage = ref('')

/**
 * 上传一个轨迹文件。
 * 组件只负责选文件，请求由 App 统一发 —— 和 selectTrack 一样的规矩。
 */
async function importTrack(file) {
  importing.value = true
  importMessage.value = ''
  tracksError.value = ''
  try {
    const form = new FormData()
    form.append('file', file)
    const res = await fetch('/api/tracks/import', { method: 'POST', body: form })
    const data = await res.json()
    if (!res.ok) throw new Error(data.error || 'HTTP ' + res.status)

    await loadTracks()
    if (!data.skippedDuplicate) {
      importMessage.value =
        `导入成功：${data.name} · ${data.pointCount} 点 · ` +
        `${(data.distanceM / 1000).toFixed(2)} km · ` +
        `${Math.floor(data.durationS / 60)}分${String(data.durationS % 60).padStart(2, '0')}秒` +
        (data.outlierCount > 0 ? ` · 标记 ${data.outlierCount} 个疑似漂移点` : '')
      await selectTrack(data.id)
    } else {
      importMessage.value = `这条轨迹已经导入过了（id=${data.id}）`
      await selectTrack(data.id)
    }
  } catch (e) {
    importMessage.value = ''
    tracksError.value = '导入失败：' + e.message
  } finally {
    importing.value = false
  }
}
```

在 template 里的 `<TrackList ... />` 上加一个事件监听：

```html
      <TrackList
        :tracks="tracks"
        :selected-id="selectedId"
        :loading="tracksLoading"
        :error="tracksError"
        @select="selectTrack"
        @import="importTrack"
      />

      <p v-if="importing" class="tip">正在导入…</p>
      <p v-else-if="importMessage" class="import-ok">{{ importMessage }}</p>
```

在 `<style scoped>` 末尾追加：

```css
.import-ok {
  margin: 6px 0 0;
  font-size: 11px;
  line-height: 1.5;
  color: #7ee0a6;
  word-break: break-all;
}
```

- [ ] **Step 3: 构建验证**

```powershell
cd frontend; npm run build
```

⚠️ 需要提权 `danger-full-access`（Vite 会 spawn 子进程，沙箱内报 `spawn EPERM`）。

Expected: `✓ built in ...`，且**包体积比上次变大**（新代码真的被编译进去了——只看"构建成功"不算验证）。

- [ ] **Step 4: 浏览器里走一遍**

启动前后端 → 打开 `http://localhost:5173` → 点「导入轨迹」→ 选
`D:\Calcite-note\GPX-Data\2026年6月22日户外跑步.gpx.bin_tmp` → 等待 → 应看到：

- 摘要显示"导入成功：… · 2342 点 · 3.93 km · 39分08秒 · 标记 N 个疑似漂移点"
- 地球上画出操场跑圈
- 底部速度曲线正常，**海拔面板显示「这条轨迹没有海拔数据」**
- 播放条能拖动、能播放

- [ ] **Step 5: 提交**

```bash
git add frontend/src/components/TrackList.vue frontend/src/App.vue
git commit -m "feat(import): 前端导入按钮 + 上传 + 结果摘要"
```

---

## Task 11: 端到端验收

**Files:**
- Create: `.tmp/check-import-pixels.py`

- [ ] **Step 1: 写浏览器验收脚本**

创建 `.tmp/check-import-pixels.py`：

```python
# -*- coding: utf-8 -*-
"""导入功能的浏览器验收：真实等待 + DOM 断言 + 像素分析。

Cesium 几何体在 web worker 里异步生成，虚拟时钟截图会漏掉线，必须真实等待。
用法：
    $env:PYTHONIOENCODING='utf-8'
    & "E:\\python\\python_address\\python.exe" .tmp\\check-import-pixels.py
"""
import asyncio
import sys

from PIL import Image
from playwright.async_api import async_playwright

URL = "http://localhost:5173/?track=2"
GPX = r"D:\Calcite-note\GPX-Data\2026年6月22日户外跑步.gpx.bin_tmp"
VIEW_W, VIEW_H = 1600, 900
SHOT = ".tmp/import-shot.png"
results = []


def check(name, ok, detail=""):
    results.append((name, bool(ok), detail))
    print(("  \u2713 " if ok else "  \u2717 ") + name + ("  " + detail if detail else ""))


async def main():
    async with async_playwright() as p:
        browser = await p.chromium.launch(channel="chrome", headless=True, args=["--no-sandbox"])
        page = await browser.new_page(viewport={"width": VIEW_W, "height": VIEW_H})
        errors = []
        page.on("console", lambda m: errors.append(m.text) if m.type == "error" else None)
        page.on("pageerror", lambda e: errors.append(str(e)))

        await page.goto("http://localhost:5173/", wait_until="load")
        await page.wait_for_selector('[data-testid="import-input"]', timeout=30000)

        # 1) 上传按钮存在
        check("导入按钮存在", True)

        # 2) 上传真实 GPX
        await page.set_input_files('[data-testid="import-input"]', GPX)
        await page.wait_for_selector(".import-ok", timeout=60000)
        await page.wait_for_timeout(6000)
        summary = (await page.text_content(".import-ok")) or ""
        check("摘要显示 2342 点", "2342 点" in summary, summary.strip()[:80])
        check("摘要显示 3.93 km", "3.93 km" in summary, "")

        # 3) 海拔面板走降级路径
        empty = await page.query_selector(".empty")
        empty_text = (await empty.text_content()) if empty else ""
        check("海拔面板显示「没有海拔数据」", "没有海拔数据" in empty_text, empty_text.strip())

        # 4) 轨迹线画出来了（像素）
        await page.screenshot(path=SHOT)
        img = Image.open(SHOT).convert("RGB")
        px = img.load()
        hits = 0
        for y in range(90, 820):
            for x in range(400, VIEW_W):
                r, g, b = px[x, y][:3]
                if abs(r - 127) < 40 and abs(g - 209) < 40 and abs(b - 255) < 40:
                    hits += 1
        check("轨迹线像素 > 500", hits > 500, "实得 " + str(hits))

        check("控制台零报错", len(errors) == 0, "; ".join(errors[:3]))
        await browser.close()

    failed = [r for r in results if not r[1]]
    print("")
    print(f"{len(results) - len(failed)} 项通过，{len(failed)} 项失败")
    if failed:
        sys.exit(1)


asyncio.run(main())
```

- [ ] **Step 2: 跑验收脚本**

需要提权 `danger-full-access`（Playwright 要建命名管道）。

```powershell
$env:PYTHONIOENCODING='utf-8'
& "E:\python\python_address\python.exe" .tmp\check-import-pixels.py
```

Expected: `5 项通过，0 项失败`。

- [ ] **Step 3: 跑全部回归**

```powershell
# 后端
& "...\mvn.cmd" -B "-Dmaven.repo.local=..." -f "...\backend\pom.xml" test
# 前端纯逻辑（必须仍全绿）
cd frontend; npm run check:playback    # 17 项
npm run check:chart                    # 37 项
```

Expected: 后端 `BUILD SUCCESS`；前端 `17 项通过，0 项失败` 和 `37 项通过，0 项失败`。

- [ ] **Step 4: 提交验收脚本**

```bash
git add -f .tmp/check-import-pixels.py
git commit -m "test(import): 导入功能的浏览器像素验收（5 项）"
```

---

## Task 12: 同步文档与会话记忆

**Files:**
- Modify: `docs/superpowers/specs/2026-09-08-calcite-trajectory-analysis-design.md`
- Modify: `_session_context.md`

- [ ] **Step 1: 更新项目总设计的 4 处差异**

按设计文档 `2026-09-12-m1-import-design.md` 第十二节：

1. **第 276-289 行**（规划的文件结构）：把「按功能分包」改成实际采用的按层分包，并列出 `service/`、`service/importer/`
2. **第 116 行**：把「`POST /api/tracks/import` 支持 GPX / CSV 文件上传」改成「支持 GPX 文件上传（CSV 暂不做，见 2026-09-12 设计文档）」
3. **第 353 行**附近：把「vivo 健康数据大概率是 WGS84，导入时需校验」改成已校验的结论：
   > vivo 健康数据已实测确认为 **WGS84**（2026-09-12：轨迹中心 `25.0336, 117.0209` 与 OSM「东区操场」坐标相差 10 米；若是 GCJ-02 会偏 400-600 米）
4. **第 4.4 节**补一条：异常速度的处理方式（存下来 + `is_outlier` 标记 + 自适应阈值 `max(8, 3×中位数)`）

- [ ] **Step 2: 更新 `_session_context.md`**

在「### M1 速度/海拔曲线（2026-09-09 完成）」之后插入新小节：

```markdown
### M1 轨迹导入（2026-09-12 完成）—— M1 至此全部完成
- 设计文档 `docs/superpowers/specs/2026-09-12-m1-import-design.md`；实施计划 `docs/superpowers/plans/2026-09-12-m1-import.md`
- 新增：`service/importer/`（`RawPoint` 统一中间结构 + `Importer` 接口 + `FormatDetector` + `GpxImporter` + `GeoLifeImporter`）、`service/GeoUtils`、`service/TrackCleaner`、`service/ImportService`、`web/ImportController`
- 改动：`track_point` 加 `is_outlier` 列（脚本两处写法保证重跑即对齐）、`Track`/`TrackPoint` 补公开构造器、`TrackPointDto` 暴露 `outlier`、`application.yml` 加批量插入参数与导入配置
- 两个入口：`POST /api/tracks/import`（网页上传 GPX）、`POST /api/import/geolife`（本地目录批量，配合幂等循环调用）
- **格式识别按文件内容**，不看扩展名（实测样本的扩展名是 `.gpx.bin_tmp`）
- 清洗规则集中在 `TrackCleaner`：排序重编号 / 速度计算 / **自适应阈值** `max(8, 3×中位数)` 标记异常（两端都标）/ **海拔整条全同 → 全部 NULL**
- 幂等键：文件内容的 **SHA-256 前 32 位**（改名也认得）
- **后端首次引入 JUnit 单元测试**（JUnit/surefire/Mockito 均在本地 `.m2`，离线可跑）：`mvn test` 一条命令
- **坐标系已实测确认 WGS84**：轨迹中心与 OSM「东区操场」相差 **10 米**（GCJ-02 会偏 400-600 米）
- 明确未做：CSV 上传、上传进度条、拖拽、异步任务、坐标系自动转换
- 待办：GeoLife 数据集下载（挂机），到位后用 `POST /api/import/geolife` 灌 5-10 个用户
```

- [ ] **Step 3: 提交**

```bash
git add docs/superpowers/specs/2026-09-08-calcite-trajectory-analysis-design.md _session_context.md
git commit -m "docs: M1 轨迹导入完成，同步总设计 4 处差异与会话记忆"
```

---

## 完成判据（全部打勾才算 M1 收尾）

- [ ] `mvn test` 全绿（后端 30+ 项）
- [ ] `npm run check:playback` 17 项全绿
- [ ] `npm run check:chart` 37 项全绿
- [ ] `.tmp/check-import-pixels.py` 5 项全绿
- [ ] 真实 GPX 上传成功：2342 点、3.93 km、39分08秒、异常点 > 0
- [ ] 海拔面板显示「这条轨迹没有海拔数据」而不是贴底直线
- [ ] 同一文件重复上传不产生第二条轨迹
- [ ] GeoLife 自造样本解析正确（点数、英尺→米、时间）
- [ ] 项目总设计的 4 处差异已同步
- [ ] `_session_context.md` 已记录

## 自查记录

### 自查中发现并已修正的 3 个问题

| # | 问题 | 修正 |
| --- | --- | --- |
| 1 | `GpxImporterTest` 里把「纬度最小值 25.032777」当成了「第一个点的纬度」——**这两个不是一回事**（那只是 2342 个点里最靠南的） | 查真实文件，首点实为 `25.0342562, 117.0204852` |
| 2 | `GeoUtilsTest` 断言「真实样本首两点相距 78 米」——**完全错了**，实测只有 **0.078 米**（起跑前站着不动） | 改为断言 0.078 米（±0.005） |
| 3 | `ImportController` 用 `@Value("${calcite.import.allowed-roots:}") List<String>` 绑 YAML 列表——**Spring 绑不了**，启动时会报占位符解析失败 | 改用 `@ConfigurationProperties` 建 `ImportProperties` |
| 4 | **（实施前评审发现）** 批量导入方法标 `@Transactional` 且内部调用本类的 `importBytes`——Spring 内部自调用不走代理，整批会退化成一个巨型事务，**一条坏数据会让后面所有文件全部失败** | 外层去掉 `@Transactional`，改用 `TransactionTemplate` 每个文件单独开事务 |

### 检查项

- [x] 无 TBD / TODO / 占位步骤，每步都有完整代码或完整命令
- [x] Task 9 步骤编号连续（1-8），插入新步骤后无重号
- [x] 类型一致：`RawPoint` / `ParsedTrack` / `CleanedTrack` / `ImportResult` / `GeoLifeImportResult` 的字段名在全部任务中一致
- [x] 构造器签名一致：`new Track(name, source, externalId, start, end)`、`new TrackPoint(trackId, seq, recordedAt, elevationM, speedMps, geom, outlier)` 在定义处与使用处一致
- [x] `outlierCount = 8`、被标点 seq `[1128,1129,1135,1136,1144,1145,1585,1586]` 均由真实数据代入算法算出，非猜测
- [x] 覆盖设计文档全部章节：范围(§2)→Task 1-9；架构(§3)→Task 3/8；接口(§4)→Task 9；数据模型(§5)→Task 1；清洗(§6)→Task 7；坐标系(§7)→Task 12；前端(§8)→Task 10；错误处理(§9)→Task 8/9；测试(§10)→各任务 + Task 11；风险(§11)→Task 5（英尺/时间）与 Task 9（批量插入）
- [x] 每个任务都能独立编译并跑通自己的测试
