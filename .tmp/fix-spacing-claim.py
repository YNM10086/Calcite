# -*- coding: utf-8 -*-
r"""把「采样间距 3~15 米」这个【被实测推翻】的前提，在所有文档里更正过来。

背景：Task 3 的子代理发现 track 6 vs 18 的 fwd 实测是 43% 而不是 100%。
主控核实后确认：
  - 采样间距真实范围是 **5~42 米**（不是 3~15 米）
  - 点对点对稀疏轨迹会严重低估 fwd（100% → 43%）
  - **但相似度（取小）不受影响**：六组实测与真值差 ≤ 0.6 个百分点

用法：
    $env:PYTHONIOENCODING='utf-8'
    & "E:\python\python_address\python.exe" .tmp\fix-spacing-claim.py
"""
import io
import sys

DESIGN = "docs/superpowers/specs/2026-09-17-m2-similarity-design.md"
PLAN = "docs/superpowers/plans/2026-09-17-m2-similarity.md"

EDITS = [
    # ---------- 设计文档 §3.3：容差理由里的间距 ----------
    (DESIGN,
     "- GeoLife 采样间隔 1–5 秒，城市骑行速度下相邻采样点间隔 **3–15 米** —— 远密于 50 米\n"
     "- 50 米**足以容纳 GPS 漂移**（城市峡谷里常见 10–30 米）",
     "- **实测采样间距是 5~42 米**（见 2.6 节）—— 不是设计时以为的 3~15 米。\n"
     "  50 米容差对密采样的轨迹（5~21 米）是合适的；对稀疏轨迹（42 米）会低估 `fwd`，\n"
     "  但**相似度（取小）不受影响**（见 2.7 节）\n"
     "- 50 米**足以容纳 GPS 漂移**（城市峡谷里常见 10–30 米）"),

    # ---------- 设计文档 §7 配置注释 ----------
    (DESIGN,
     "    # 默认容差（米）。GeoLife 采样间隔 3~15 米，城市 GPS 漂移 10~30 米\n"
     "    default-tolerance-m: 50",
     "    # 默认容差（米）。实测采样间距 5~42 米、城市 GPS 漂移 10~30 米（见设计 2.6 节）\n"
     "    default-tolerance-m: 50"),

    # ---------- 设计文档 §10 已知限制那一行 ----------
    (DESIGN,
     "| **⚠️ 用「采样点」代表折线** | SQL 判的是\"B 有没有**采样点**落在我 50 米内\"，**不是**\"我离 B 的**线**有多远\"。B 采样稀疏时会**低估**重合度 | GeoLife 是 1–5 秒采样，城市里相邻点只隔 3–15 米，**远密于 50 米容差**。用折线距离就要放弃索引：实测 **119 秒 vs 2 秒**。**代价写出来，不假装它等价** —— 第 9.3 节有专门测试量化它 |",
     "| **⚠️ 单向百分比是近似值** | SQL 判的是\"B 有没有**采样点**落在我 50 米内\"，不是\"我离 B 的**线**有多远\"。对方稀疏时会**严重低估 `fwd`**（实测 track 6→18：100% 被算成 **43%**） | **相似度（取小）不受影响** —— 被低估的方向恰好是本来更大的那个（六组实测差 ≤ 0.6 个百分点，见 2.7 节）。点到折线单对 61.5 ms vs 点对点 3.3 ms（**19 倍**），241 条候选就是 12.8 秒 vs 0.79 秒。**代价写出来，并用测试钉住\"相似度仍准确\"** |"),

    # ---------- 设计文档 §11.3 ----------
    (DESIGN,
     "第 3.3 节：GeoLife 采样间隔 3–15 米（远密于 50 米，所以点对点近似成立）；",
     "第 3.3 节：**实测采样间距 5~42 米**（不是设计时以为的 3~15 米）；"),

    # ---------- 设计文档 §9.3 专项 ③ ----------
    (DESIGN,
     "**③ 点对点近似 vs 点到折线（第 10 节那条限制的量化）**\n\n"
     "独立实现**真正的点到折线距离**（用 Python + shapely 或 PostGIS 的逐点写法），\n"
     "和接口的点对点结果对比，**断言两者差距在可接受范围内**，并把差距打出来。",
     "**③ 点对点近似 vs 点到折线（第 10 节那条限制的量化）**\n\n"
     "独立实现**真正的点到折线距离**（PostGIS 的逐点写法），和接口对比，\n"
     "**断言【相似度】与真值接近（≤ 2 个百分点）**，并把差距打出来。\n\n"
     "> ⚠️ **不要断言 `forwardPct` 与真值接近** —— 它本来就对不上（稀疏轨迹下会被低估\n"
     "> 几十个百分点，实测 100% → 43%），而且**不该对不上**：`min` 取的是另一个方向。\n"
     "> 断言的对象必须是**相似度**。"),

    # ---------- 计划 Step 8 的期望值 ----------
    (PLAN,
     "**Expected**：如果出现，`forwardPct` = **100**、`reversePct` ≈ **35**、**`similarity` ≈ 35**。\n"
     "**如果 `similarity` 是 100，说明\"取小\"没生效 —— 立刻停下来查。**",
     "**Expected（实测值）**：`forwardPct` ≈ **43.0**、`reversePct` ≈ **34.4**、**`similarity` ≈ 34.4**。\n\n"
     "> ⚠️ **`forwardPct` 是 43 而不是 100，这是【正确的】** —— track 18 的采样间距是 42 米，\n"
     "> 点对点会低估 `fwd`（真值 100%）。**但相似度取的是另一个方向（`rev` 34.4）**，\n"
     "> 所以结果 `34.4` 与真值 `35.0` 只差 0.6 个百分点。详见设计文档 2.6 / 2.7 节。\n\n"
     "**如果 `similarity` 接近 100，说明\"取小\"没生效 —— 立刻停下来查。**"),

    # ---------- 计划 Step 9 ① 的期望 ----------
    (PLAN,
     "**Expected**：① `matches` 为空且不报错；② **400**；③ **404**；④ 返回 3 条但 `compared` 仍是 197。",
     "**Expected**：② **400**；③ **404**；④ 返回 3 条但 `compared` 仍是 197。\n\n"
     "> ⚠️ **① 的期望要改**：3 条 GPX 轨迹是**同一场地**跑的，它们**互相之间很相似**，\n"
     "> 所以 `matches` **不为空**（实测如此）。要验证\"空数组不是错误\"这条路径，\n"
     "> 改用一个**苛刻的容差**：`?trackId=1&toleranceM=1` → 期望 200 + `matches: []`。"),
]


def apply(path, old, new):
    with io.open(path, encoding="utf-8") as fh:
        t = fh.read()
    n = t.count(old)
    if n != 1:
        print(f"  [XX] {path.split('/')[-1]} 期望命中 1 次，实得 {n}：{old[:56]}…")
        return False
    with io.open(path, "w", encoding="utf-8", newline="") as fh:
        fh.write(t.replace(old, new))
    print(f"  [OK] {path.split('/')[-1]}  {old[:50]}…")
    return True


def main():
    ok = True
    for path, old, new in EDITS:
        ok = apply(path, old, new) and ok
    print()
    print("全部替换成功" if ok else "有替换失败")
    if not ok:
        sys.exit(1)


main()
